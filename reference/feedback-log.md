# Feedback Log — issues Parker caught while studying, and how they were resolved

The running record of the study → catch → fix loop. Newest at the top. Each entry: **what Parker flagged**, the **fix**, and whether it became a permanent **rule + regression test** (systemic) or was a one-off. A fresh Claude session can read this to see the live history of what's been refined.

Parker reports issues two ways: **(a) tell Claude directly** in any session ("I noticed an issue with an EMT card…") — easiest, and Claude logs it here; or **(b) jot a quick line at the top of this file** between sessions, and Claude processes it next time.

---

- **2026-07-31 — Chapter 7 generation run (53 marks → 59 notes / 91 cards): two margin comments honored, and the numeric safety flag found to be partly dead code.** Not a card Parker caught — a full pipeline run whose own checks surfaced the problems.
  1. **The numeric safety flag was missing whole classes of value (the real find).** `profiles/emt.md` §6 requires `needs_human_check: true` on *every* number, and `needs_human_check` is derived from a single regex. That regex had three holes: a trailing `\b` after `%` meant **the percentage branch could never match at all** (dead since it was written); ages stated in months/years — how a life-span chapter states nearly every fact — carried no recognised unit; and rates written `140 beats/min` fell between the digits and the `/min` token. Cards were hand-asserting `numeric: true` and `verify_report.py` was deriving it straight back to `false`, filing them under "verified, skim". Found independently by two of the eight unit editors. *Systemic:* widened `check_cards.VALUE`, made the checker respect `verified_against` so it stops disagreeing with `verify_report` on every verified table card, and added regression **R24** (five cases, both directions). Deliberately still NOT flagged: the hyphenated patient descriptor (`a 2-month-old`), because a vignette age is invented scene-setting and flagging it would dilute the section that exists to concentrate his attention — recorded as a scope decision in R24 rather than left as a silent hole.
  2. **The figure matcher scores CLOZE ANSWERS only, never visible text** — counter-intuitive enough that this run's own briefing told drafters the opposite. A card can name its subject three times in prose and score 0.00. Parker's margin comment on p690 asked for a high-definition FIGURE 7-2 "into the flashcards relating to the fontanelles" (plural), and only one of the three could ever match. *Fix:* documented in `SKILL.md` Stage 2.9 with the standing instruction never to write a card to feed the matcher, plus the sanctioned escape hatch — a hand-added `forced: true` proposal that still goes through the judge. All three fontanelle cards carry the plate.
  3. **His other margin comment (p688) asked for a "guess all the things different in an infant mouth than an adult" card** — built as a four-row grouped reveal. Deliberately states no count: the source says "due to factors *such as*", and his own preference forbids carding an open list as a closed set, so the four visible blanks give him the count without the card claiming the book closed it.
  4. **A contradiction in the textbook itself**, surfaced rather than papered over: p683 prose calls 60 breaths/min "usually normal for an infant" while TABLE 7-1 caps the infant band at 25 to 50 (60 is the *neonate* band). The affected card was reworded off the disputed digit; flagged to Parker at hand-off. No check can adjudicate a book against itself, so this is recorded as unmechanizable in the run manifest.

- **2026-07-30 (correction, same day) — the "fabricated ETHICS mnemonic" was PARKER'S OWN WORK, and deleting it exposed the real gap: nothing recorded who wrote what.** The entry below reports catching a fabrication. That was wrong, and the error is worth more than the fix was.
  **What actually happened:** rebuilding the ethical-checklist card meant promoting its ETHICS mnemonic from a Back Extra aid to the card's answers, so it was verified against the textbook first — and found nowhere in it, along with a question ordering that differed from TABLE 3-2. The conclusion drawn was "a previous run fabricated this and reordered the book to fit." The card was replaced with the book's wording and order. **Parker had written the mnemonic himself** and reordered the questions deliberately so the letters would spell. His words: *"that was totally me… if you just deleted that that would be really bad."* Restored from git (`a256ce2`), live and in canon.
  **Two distinct failures:**
  1. **A false dichotomy — "in the source" vs "fabricated" — that omits the deck's owner.** There is a third author, and he is the one studying from it. Absence from the textbook is NOT evidence of fabrication. It is equally evidence of Parker.
  2. **No baseline existed to check against, and the obvious candidate was already poisoned.** Canon files get resynced from live during audits, so they had long since absorbed his edits; canon could not answer "did we write this?" Anki's own `mod` timestamps can't either — bulk pipeline passes (702 notes on 07-18, 176 today) swamp the signal, though *isolated* single-note edits on quiet days do stand out, and all four such notes in the deck turned out to be his (HyperTTS audio, `&nbsp;` editor artifacts).
  **The fix — `scripts/authorship.py`, a write-time fingerprint store.** The pipeline hashes exactly what it writes, when it writes it (wired into `anki_write.py`). Before any later write, the live field is hashed and compared: `owned` may be overwritten, **`edited` and `unknown` may not.** **Silence is not consent** — all 1,099 pre-existing cards are `unknown` and therefore protected, which is the correct default for the failure being fixed. Verified whitespace-only repairs still pass (`is_whitespace_only()` checks the claim rather than trusting the caller), so the layout rules stay enforceable. `authorship.py self-test` covers all 8 branches; `scan` gives a retroactive best-effort map of where his work most likely is (614 fields bear a hand-edit mark). Encoded in `parker-preferences.md` (top of Confirmed), `SKILL.md` step 4, and `CLAUDE.md`.
  *Lesson:* the deeper error wasn't the deletion, it was the confidence. A verification step that can only return "supported" or "fabricated" will eventually delete the user's own work and file it as a win. **When a card contains something the source doesn't, the first question is "who wrote this?" — not "is this real?"** And a system that mutates a user's data needs to know what it authored before it is allowed to overwrite anything.

- **2026-07-30 — the Cold-Solve Gate was enforced per CARD; the failures were per ROW. Three complaints → three rules, three detectors, 15 regression cases, and a duplicated-predicate bug that had silenced R14 the same day it shipped.** Parker harvested three `Card Feedback` rants and then set the priority explicitly: *"I want this ambiguity to be cut out… what matters so much more is that this is a systemic issue and that it gets fixed."* So the mechanization was the deliverable and the three cards were the footnote.
  **The three complaints, and the distinct class each exposed:**
  1. **Ch4 EMS-radio card (#1784513284017) — the row label restates its own answer (→ R15, card-rules #20).** `Arrival at hospital or point of transfer → {{c1::notify dispatch of arrival}}`; `Return to service → notify dispatch when {{c1::the unit is available for another call}}`. Parker: "the return to service is the thing I'm supposed to say, so you're giving away the answer while trying to give me a hint." The SAME card carried the mirror defect on its last row — `Miscellaneous → {{c1::not in station}}`, a label cueing nothing (R9 open-set). Both are row-level; the card reads fine whole, which is exactly how it passed.
  2. **Ch4 "never attribute altered mental status to {{c1::old age}}" (#1784513279031) — R9 open-set, recurring (→ R16, card-rules #21).** Parker: "there are a lot of things I could fit in that blank… since there was no hint, no other cues, nothing else, how am I supposed to know it?" **Why it came back:** R9 was ruled mechanically undetectable in July and left judge-only. Chapters 4 and 6 were generated after that ruling. *A rule that only a judge enforces is a rule that recurs* — that is the lesson, and R16 is the first mechanical proxy for its commonest disguise (an absolute/prohibition sentence with one unhinted blank).
  3. **Ch3 "8 self-check questions" (#1782941722678) — inverted cue and answer (→ R17, card-rules #22).** All 8 question-frames visible, one guessable word punched out of each. Parker diagnosed it better than any check could: "it doesn't actually help me remember this card, it just helps me remember the CONTEXT of the card." The source settles the archetype — the book calls these *"general questions when you are faced with making a decision such as this one"*, a deliberation aid for an abandonment call, not a memorize-these-8 list. Rebuilt as the abandonment vignette it belongs to, with the 8 questions kept in Back Extra as reference.
  **The bug underneath (R14b) — a duplicated predicate is a single point of failure that reports itself as two confirmations.** `listify()` and `packed_list_layout()` each held their own copy of "is this a list?", both asking `all(_is_list_row(s) for s in segs[1:])`. **One long row vetoed the whole card**, so the repairer AND the warning went silent together and the defect was invisible from both directions. Twelve genuinely list-shaped live cards were still packed — including the radio card Parker complained about hours after R14 shipped, and the ETHICS checklist (5 of 6 rows qualified; one 9-word row killed it). *Fixed three ways:* a second signal (a **colon-terminated lead-in** heading ≥2 cloze-bearing lines is a list, because the author announced one — that beats any word count); `_has_single_br_separator` replacing the coarse "contains no `<br><br>`" test that had let MIXED cards pass; and `anki_write.listify()` now **imports `list_shaped` from `check_cards`** so the two can never drift again. All 12 repaired live.
  **A fabrication caught in passing — and the blind spot that let it live.** Rebuilding the ETHICS card meant promoting its mnemonic from a Back Extra aid to the card's *answers*, so it got verified first: **the ETHICS acronym is nowhere in the textbook.** A prior run invented it AND reordered TABLE 3-2's six real questions to make the letters spell. Rebuilt honestly from the table in the book's own order. **Why R13 never caught it:** grounding needs `from_idx`, and only **307 of 1,101** live cards carry it — Chapters 1, 2, 3 and 5 (792 cards, 72%) are pre-provenance and therefore structurally invisible to the one check designed to catch fabrication. *Open for Parker:* an untracked `scripts/backfill_provenance.py` exists; backfilling those four chapters would put the deck's largest, oldest section under R13 for the first time.
  **What shipped:** card-rules **#20–22** + a standing instruction that the cold-solve test runs **per row**; editor checks **#22–24** with the same instruction; regressions **R14b, R15, R16, R17**; `check_cards.py` detectors `row_label_tautology`, `open_set_absolute`, `fragment_clozed_list`, plus the rewritten `list_shaped`/`packed_list_layout`; `test_regressions.py` **28 → 43** cases, green both directions. Volume on the live deck: **27 flagged → 2**, both judged benign and deliberately left (an incidental shared word on the guide-dog card; the liver card, whose frames are genuine cues, not filler). Eight cards fixed in total — Parker's three, plus five more of the same classes he had not hit yet (ETHICS, hypothermia "warm and dead", the older-patient assessment card, the PCR patient-count card, and the clotting row of the blood-components match card) — and 11 more relaid out.
  **One detector precision fix found by its own output:** `open_set_absolute` fired on a therapeutic-communication vignette because the patient's quoted line contained "never." Quoted dialogue is not a rule the card is stating, so quoted spans are now stripped before the absolute search — with a GOOD regression case, per the standing rule that *a detector's exemption is itself a card-rule surface* (R11's precision round).
  *Lesson (encoded):* the Cold-Solve Gate had the right rules and the wrong granularity. A multi-row card passes a whole-card read while individual rows are self-answering or wide open — so **the row, not the card, is the unit of review**, and a card is only as good as its worst row.

## Open (jot new issues here)
<!-- Parker: drop a one-liner here anytime, e.g. "- card on X: the hint feels too revealing" -->

## Resolved

- **2026-08-02 — "10 things all at once… I have to remember them all at once, and if I forget one thing I have to do it all again": retrieval load (card-rules #23–24, R25–R26).** Parker brought this as a class, not a card: *"I see this a lot happening where there will be a table that I'll highlight in Zotero and your first reaction is OK all of those go together so it needs to be one flash card with all of it being in one deletion — however it can extend beyond what's physically able for me to be memorizing all in one go."* His prime example was the 10-element radio patient report; his own response to it was avoidance — *"what I'll find myself doing is just skipping that flash card as I'm reviewing, because I know that I can't get it right."*
  *The diagnosis he was reaching for, stated plainly:* a grouped reveal is graded **all-or-nothing**. He presses one button for the whole set, so the card is "Good" only if every item lands. At a realistic 90% per item that is 0.9^N — 59% at five, 35% at ten — meaning a card built entirely from facts he knows cold still fails essentially forever, and Anki compounds it by rescheduling the whole card on its worst item. His own words name the mechanism exactly: *"it doesn't know the difference between how well I know that I'm supposed to give a special alert any more than it does that I should radio about the patient's age and sex."*
  *Measured before changing anything* (live collection, EMT decks only so generation run and maturity match, scored on each card's FIRST review so new-card bias cannot explain the gap): ordinary 1–2-blank cards failed **56%** at 24.9 s; 3–4-item groups **78%** at 33.3 s; 5–6-item groups **89%** at 42.7 s; 7+ **80%** at 49.3 s. The radio card itself: **5 reviews, 5 "Again," 54 s each — never once answered correctly.** Note that lapses show nothing here: a card that never graduates from learning never records one, which is why this stayed invisible until the raw review log was read.
  *The finding he did not expect:* the two cards he named as GOOD are failing too. **CHART** (5 items, acronym-anchored) is 3/4 Again at 49 s; the **five-point handover method** is 4/6 Again at 43 s. His reasoning about CHART is still right — the acronym is the memory anchor and splitting it would destroy the anchor — but "feels right" and "is working" came apart, and only the log showed it. Hence 5–7 is a *warning* band, not a blessing.
  *The rule (card-rules #23):* the cap is on answers carrying **no per-item cue** — ≤4 ships whole, 5–7 needs a genuine retrieval handle (a spelled mnemonic the card teaches, or a structure that regenerates the members), ≥8 must be chunked. Chunking prefers a **semantic partition** into 2–3 named sub-groups of ≤4 plus an anchor note teaching the skeleton, because the sub-group names are themselves what makes the whole set recoverable — the 10-element radio report is really 3 + 4 + 3 (dispatch header, patient picture, care and close). A **per-item cue is the cheapest fix of all** and exempts a card at any length: `Neonate — {{c1::100 to 180}}` is six cued retrievals, not one six-wide recall.
  *The trap, which Parker identified himself before it was written down (card-rules #24):* the obvious fix is to renumber the list c1..cN, and that is worse than the disease. *"I don't think that's what we need… if it's a hide-one-but-show-all-the-rest, I can just figure that out."* N sibling numbers on one note produce N cards that each display the other N−1 answers, so the set is recovered by elimination. Chunking always means separate NOTES. This is HARD-blocked so a future pass cannot reason its way into it while trying to obey #23.
  *His other request, adopted verbatim:* every note born from a chunked set carries a **`Roster:`** line in Back Extra with the full set in order and its own members bolded — *"seeing the part and the whole in each flash card."* New blessed Back Extra label.
  *Mechanized, not just written:* `overloaded_group()` (≥6 uncued warns, **≥8 HARD-blocks**; spelled-mnemonic license reuses R11's exact predicate so the two detectors cannot disagree) and `sibling_split_leak()` (HARD) in `check_cards.py`; card-rules #23–24; editor checks #25–26; regressions **R25–R26** with 11 executable cases (suite 48 → 59). **The thresholds were calibrated against Parker's own four verdicts and reproduce all of them:** radio report → HARD, CHART → clean, five-point handover → clean, six-cause hostility → warn (his "you could argue it either way"). Live blast radius: **6 HARD, 8 warn across 1158 EMT notes**, and **zero** sibling-split false positives on a deck built entirely under the old rules.
  *Superseded:* the 2026-06-29 "keep a real list in ONE card **no matter the length**" clause, which is what generated these cards. Cohesion survived — a real set is still one concept and must never be scattered into unrelated shards — but one concept is no longer automatically one card.
  *Applied:* the 6 HARD-blocked cards were rebuilt as **19 chunked notes** and staged into their chapters' `claude review` decks (ch3 +6, ch4 +10, ch6 +3); the originals were left in place untouched for Parker to suspend or delete after he compares them. All six chapter canons re-gated and re-stamped; smoke 32/32, regressions 61/61, figures 4/4. The 8 warn-band cards were left alone — they are judgment calls (Star of Life, the ETHICS checklist, the cranium bones) and several are ones he may well want kept whole.
  *Two things the drafting turned up that the rule did not anticipate, now folded in:* (1) an **anchor note must not cloze a partition the pipeline invented** — the first drafts clozed "the dispatch header", "two failure modes", "the patient's baseline standing", phrases in no book, which are unanswerable cold and teach a taxonomy the NREMT never asks about. All four were dropped; the partition names stay as VISIBLE scaffolding in each sub-group note's stem, and the radio family's anchor became a grounded order vignette built on the book's own bathtub-fall patient. (2) Rules 14 and 23 **fought over the stated count** — a chunk note truthfully says "10 elements" while clozing three — and the first draft resolved it by writing "10-element" to dodge the regex. Checker-shaped phrasing is the smell that two rules are in conflict and the human is absorbing it; fixed with a `Roster:`-keyed exemption, tested in both directions.
  *Also fixed, unprompted findings from the same drafting:* two ungrounded claims in the ORIGINAL Chapter 3 Back Extras (the book says decapitation is *an example of* obvious mortal damage, not a coordinate item; and "resuscitation may be warranted" is not on p295 at all), and Chapter 4's improper-interview partition was re-cut 5/3 → **4/4** against the book's own Rationale column, since authoritative language fails on communication, not respect.

- **2026-08-02 — two process failures, both stale reads (R27).** Recorded because they cost real cleanup and neither is visible in any card. (1) Three drafting agents ran in parallel; their outputs were reviewed and edited, then **two agents rewrote their files afterward**, so the staging step read the pre-edit content and wrote **three already-rejected cards into the live deck**. Caught by reconciling staged files against canon, and deleted. The rule: after a fan-out, treat agent output as immutable input and re-verify at the moment of use — and always reconcile what you staged against canon before declaring success. (2) A smoke assertion pinned a live card COUNT (`live ch3 = 84 cards`), so staging six notes turned the suite red for a non-problem. A green suite must mean "the path works," never "nobody has staged anything since." Rewritten as a range; genuine source-parity numbers (ch1's 36 marks, ch5's 587 notes) correctly stay exact.

- **2026-07-30 — "a list should look like a list": list-shaped Text now gets a blank line between rows (card-rules #19, R14).** Parker sent before/after screenshots of the Chapter 4 SBAR card. The generated version packed four blanks into one block with single `<br>`s; he had re-laid it out by hand, pressing enter twice between rows. His reason, which is the rule: *"for me to be able to be prompted to guess these questions and then immediately see in a very clear way how many things I need to guess."* He answers a list card by first counting the blanks, then producing them — and a packed block hides the count, which is the single most useful thing on the front. *"It's not in a sentence. It's not doing anything crazy. It's literally just a list."*
  *This is the Text-field twin of the 2026-07-02 Back Extra preference* (paragraph breaks between labeled components), and it got the same three-layer treatment rather than prose alone: **rule** (card-rules #19, editor check #21, parker-preferences, card-recipes §6 template), **mechanization** (`listify()` in `anki_write.py`, the counterpart to `paragraphize()`, so the spacing holds even if a card is drafted tight), and **test** (regression R14 + 4 executable cases; suite 24 → 28).
  *The discriminator — what makes this safe to automate:* a **row** is a line that carries a cloze and almost no prose of its own (≤8 residual words; numbering and bullets are layout, not prose). A card qualifies when it has ≥2 rows and everything after the lead-in is a row. That distinguishes a genuine list from a card that merely uses `<br>` to separate two flowing sentences, which must stay untouched. Verified across the whole live EMT deck: of 84 cards containing `<br>`, **43 were restructured and 41 were correctly left alone**; the transform is idempotent.
  *Applied:* 43 live Anki notes and 45 canon cards across Ch1-4 and Ch6 (Ch5's word-root cards contain no list layouts). The 2-card difference is the pair Parker had already deleted from Ch4. All six chapter canons re-gated and re-stamped; `--live all` now reports zero packed-list warnings.

- **2026-07-29 — post-mortem of the Chapter 6 run: Rule 1 was never machine-checked, and provenance was being computed then thrown away.** Parker asked for a code-review of the Ch6 output, working backwards from the finished cards into the repo. Ch6 itself is the best chapter the pipeline has produced — 197/197 marks covered (independently verified), 10 mechanical warnings all in known-benign classes, every spot-checked numeric exact against the AAOS pages, and the drop in hint usage (44% → 19%) turned out to be *better* craft, not a regression: Ch6 anchors paired contrasts structurally instead of leaning on `::hints`.
  *The real finding was about the process, not the cards.* Three tables (6-3, 6-10, 6-12) produced 23 cards. `pdftotext` returns 505 characters for the whole of p607 — TABLE 6-10's body is a rendered image — and TABLE 6-3's body sits on p549 while its caption is on p548. **31 table/figure-title highlights exist across ch1-6 and ZERO triggered the extractor's next-page widening**, because `list_lead_in`'s regex doesn't match a title. Worse, `grounding: EXACT` was reported for all of them: it only ever answered *"did I locate your marked text?"*, never *"do I have the material?"* The clearest case is Ch4's TABLE 4-4, `grounding: EXACT` with a 511-character context about not touching a patient's torso.
  The Ch6 cards turned out to be **correct** — agents were told by the run's brief to go read the table pages, and did; every muscle-table card matches p549 verbatim. But **nothing recorded that**: all 23 had `image: null`, no page render survived, and the only trace was a stray remark inside an ad-hoc `_orchestrator_edits.json`. From the artifacts alone, a correct card was indistinguishable from a fabricated one — the review spent twenty minutes on archaeology to tell them apart.
  *Fixes, all mechanized rather than described:*
  - **R13 — the first mechanical enforcement of Rule 1.** Cards now carry `from_idx`, so `check_cards.py` can test every cloze answer against the context of the mark it cites. An answer absent from a `needs_visual` mark with no attached evidence is a HARD block; a paraphrase shortfall is a warning for the judge. Measured on Ch6: 47 blocks, 94% citing a `needs_visual` mark. Two false-positive classes were found by testing the detector itself and fixed — morphology ("symphyses" vs "symphysis") via ≥5-char prefix matching, and paraphrase-vs-fabrication by scoping HARD to marks already known to be text-insufficient.
  - **The extractor stops conflating located with sufficient.** `TABLE|FIGURE|BOX|SKILL DRILL` titles now get the next-page pull (TABLE 6-3's context went 271 → 716 chars and gained the real muscle rows), and page text density relative to the source's own median flags image-heavy pages. New fields `content` (`FULL`/`CAPTION_ONLY`/`SPARSE_PAGE`) and `needs_visual`. The relative measure matters for generality: a 300-char lecture slide and a 2,000-char textbook page are each normal for their own document.
  - **`needs_human_check` is derived, not asserted.** It meant "contains a digit" and fired on 35% of Ch6 (72 cards); the run's own report conceded it was "a confirmation pass, not a list of suspected errors." Now computed as `(numeric or weak grounding) and not verified_against`, with `verify_report.py` splitting the report into **Section A: needs your eyes** and **Section B: verified, skim**. Ch6 drops from 72 to 21.
  - **Provenance is kept, because it is what makes the grounding check possible.** New `runs/<source>/<segment>/<run_id>/` holding the manifest (with the skill's git SHA), the highlights snapshot, the cards, `provenance.jsonl`, `decisions.jsonl`, **`dropped.jsonl`** (the most valuable file, which did not previously exist), and `figures/` evidence crops. `anki_write.py --run` writes each Anki noteId back into the record, so `run_store.py trace <noteId>` tells any card's whole story — and the link lives in the repo, not as a tag, because Parker keeps `ch<N>` only.
  - **The hazard rule.** `check_hazards.py` (run by `smoke_test.sh`) fails if a run records a discovered hazard without either a `regression_id` or `mechanizable: false` + `why`. This is the durable fix for the pattern that produced this whole entry: Ch6 found the table hazard, wrote a paragraph in SKILL.md, shipped the cards, and reported itself verified — exactly as the 2026-07-19 audit called 899 cards "confirmed sound" while missing the bloated-c2 class. Both were truthful about the checks that existed. *Name it, mechanize it, test it* — step one keeps getting mistaken for the whole job.
  - **Drift became a feedback channel instead of a discrepancy.** `sync_report.py` classifies canon-vs-Anki divergence by meaning: rejected (selection signal), edited (card-craft signal — the diff IS the lesson), hand-written (imitate). It immediately explained Ch4's 107-vs-105: Parker had deleted the card that crammed 8 TABLE 4-4 rows into one blob — which is also why an earlier coverage probe found only 5 of its 6 pitfalls carded.
  *Backfill:* Ch6's 202 cards regained the `from_idx`/`block` the run had computed and discarded, 15 evidence crops were rendered for the image-derived cards, and a run record was reconstructed from the `ch6_units` artifacts. Ch6 now passes R13 legitimately rather than by exemption. Regression suite 20 → 24 cases; smoke test 27 → 31 checks; all green.

- **2026-07-29 — Chapter 6 run (197 highlights → 202 notes / 377 cards) surfaced 2 systemic issues in the skill's own reference files.** Neither came from Parker; both were caught by the pipeline's own agents and would have silently degraded future chapters.
  1. **A reference file carried numbers that contradicted the textbook.** `card-recipes.md` §5's illustrative example gave pediatric respiratory rates (child 15-30/min, infant 25-50/min) that do not match AAOS 12e TABLE 6-4 (child 12-40, infant 30-60). Because `card-recipes.md` is read before drafting *every* chapter, a stale digit there can seed a wrong card in any future chapter — Patient Assessment and Respiratory Emergencies especially. *Fix:* corrected the numbers and added a standing warning that the recipes teach card SHAPE while digits always come from the page being carded.
  2. **Highlighted table captions can silently yield nothing, and then falsely report EXACT grounding.** In this textbook many table BODIES are rendered images: `pdftotext` returns the caption plus a bare "Description" stub, so an agent that only greps text gets zero rows and the table's content is quietly lost. Three of Chapter 6's tables (6-9 nervous-system CV effects, 6-10 nervous-system structures, 6-12 endocrine glands) were image-only, and content existing *nowhere* in the text layer (alpha-1; "pale, cool, clammy"; muscarinic; prefrontal area; limbic system; thalamus) was recovered only by rendering the pages. Worse, the extractor reports `grounding: EXACT` on these items because it matched the *caption* — so a verifier that greps text will wrongly condemn correct table-derived cards as fabricated (one judge nearly did). *Fix:* SKILL.md Stage 1 now tells the drafter to render the page whenever a highlighted table caption yields no rows, warns that EXACT grounding on a caption is not grounding of the body, and notes that tables are usually **recaps** — the single most common source of same-fact duplicates (card-rules #12), so card what a table *adds*, not what it repeats.
  *Also confirmed this run:* the 3-stage architecture is doing its job. Independent judges caught 16 defects the drafting agents had passed — including an outright factual error ("of the **two** epidermal layers" vs the book's "multiple layers"), three hint leaks (`::nerve fiber` for "axon"), an arithmetic leak (98% hidden while "the remaining 2%" stayed visible), an ungrounded Back Extra (hypothalamus, a word absent from the whole chapter), and two R3 etymology leaks (hiding "retroperitoneal" while "behind the peritoneum"/"behind the abdominal cavity" sat visible). The global consolidation pass found 4 cross-block duplicates and 6 cross-card give-aways that no single-block agent could have seen — 3 of the 4 duplicates traced to table recaps, per the trap above.

- **2026-07-29 — generalized from an EMT-only card maker into a universal Zotero→Anki pipeline (skill + repo renamed `emt-card-maker` → `zotero-to-anki`).** Parker's reasoning: reading and wrestling with the textbook is his best learning lever, he intends to read *every* textbook this coming semester, and the card-making machinery was already good — what limited it was that it only knew one book. Goal: point Claude at any Zotero source (an Arabic unit, a genetics lecture PDF, a paper) and get the same quality.
  *What did NOT change — deliberately:* the entire card-craft core. `card-rules.md` Layer A/B including the Cold-Solve Gate, the 20-point `editor-checklist.md`, `card-recipes.md` §2-11, `regression-cases.md` R1-R12, `check_cards.py`'s detectors, the `.verified` stamp gate, and the feedback loop are untouched. `test_regressions.py` stayed 20/20 green throughout.
  *What changed:*
  - **A source registry** (`reference/sources.json` + `scripts/sources.py`). Everything that was hard-coded to one book — the PDF, the "card me" colors, the page→segment map, the Anki decks, the tags, the note type, the subject profile — is now per-source. Attachments resolve by stable Zotero **item key** rather than a filename substring (the old `path LIKE '%Pollak%'` had already misfired once).
  - **`scripts/add_source.py`** — the first-run flow: search Zotero, show what's marked, print Parker's live deck tree so the proposed deck fits his conventions, dump a book's PRINTED contents pages, build the segment map. He confirms the deck **once**, then it's permanent (his chosen design).
  - **Subject profiles** (`reference/profiles/{emt,science,language,default}.md`). Card craft is subject-independent; *emphasis* is not. The NREMT application bias and clinical archetype mix moved out of `card-recipes.md` §1 into `profiles/emt.md`. Language flips to production-over-recognition + RTL/diacritic handling; science leads with mechanism and treats lecture margin comments as the top signal.
  - **The two-deck promotion gate is now universal, with per-source names** — every source stages to `…::claude review` and Parker promotes keepers himself; a lecture gets `Keepers` instead of book-shaped `Book Highlights`.
  - **`work/` is per-source** (`work/emt/…`), and the EMT canon gained `source`/`segment` fields (`chapter` still accepted).
  - **`scripts/smoke_test.sh`** — 27 end-to-end checks asserting card craft is untouched, EMT parity holds, and the universal path works. All green.
  *Two real discoveries the generalization forced, both bugs that would have silently returned nothing:*
  1. **"Card me" is a COLOR, not a markup style.** Parker highlights in the textbook but **underlines** on lecture slides. The extractor read only Zotero `type=1` (highlight), so the Isaacs Ch17 lecture — six yellow underlines, zero highlights — would have come back EMPTY. Now types 1 (highlight), 5 (underline), 3 (area selection → an image card, cropped to his exact box) and 6 (standalone note → surfaced, never carded blind) are all read; ink and sticky notes are skipped. `add_source.py` had the identical bug in its counting and was fixed with it.
  2. **Zotero's stored `contentType` lies.** `Isaacs Chapter 16.pptx` is registered as `application/pdf` but is a real PowerPoint (zip magic), which poppler would have turned into garbage rather than an error. The extractor now checks magic bytes and stops with the fix.
  *Also encoded:* a margin comment that countermands his own mark (**"NOT ON THE EXAM"**, "not in the slides", "these are subjective so dont just mem" — all real comments in his genetics lecture) is the ONE licensed exception to "never drop a marked span," and must be reported at hand-off. Red on lecture slides frequently carries this meaning.
  *Open for Parker:* the `isaacs17` deck path is PROVISIONAL (proposed, not confirmed) and flagged in its registry `notes`.

- **2026-07-19→20 — "find every issue and make it infinitely better": a deeper re-audit caught the whole class the prior sweep called "confirmed sound."** Parker asked for a rigorous, no-stone-unturned pass over the card maker and every past card. A first mechanical sweep of all 899 live cards (a wider battery than the gate had) plus a grounded agent fan-out (one adversarial editor per chapter, whole-chapter view for cross-card checks, every fact verified against the AAOS PDF) found that the prior "899 swept, 14 refined, everything else confirmed sound" had **missed a large systemic class**: bloated two-way-definition c2 sides. **~30 definition cards across Ch1-4 hid an 8-21-word meaning clause** — the *define-it* direction was a guaranteed cold-fail (no one recalls "a quality-management process in which team members continuously review responses to find and fix system weaknesses over time" verbatim). It slipped the earlier pass because it was neither a named checklist item nor catchable by any mechanical check.
  *What was refined — 64 cards across all five chapters (Ch1 13, Ch2 14, Ch3 32, Ch4 4, Ch5 1), + 15 numeric/one flagged `needs_human_check`:*
  - **Crisp-c2 (the dominant fix, ~30 cards):** tightened each bloated c2 to a 3-6 word discriminating core, kept the two-way structure, pushed the fuller wording to the Back Extra. (R12.)
  - **Husks split to c1/c2 (R10/R10b):** POLST/MOLST expansion, Expressed=actual synonym, child-accompany (who + how-often), cleaning-gloves (wear + never-use). The three visible-anchor contrasts the checker also husk-flags (cover/concealment, low-crouch/sitting, one-drink/two-drinks) were correctly LEFT — they are answerable, not husks.
  - **Cross-card give-aways removed (R6):** expressed-consent / assault / false-imprisonment definition cards had `Ex:` lines that spelled out the exact answers to their sibling scenario cards.
  - **Fuzzy scenario→action clozes tightened (R8):** religious-objection ("report it to the {{c1::next level of care}}"), grieving-wife, highway cards — crisp load-bearing words, scenario left visible. (The religious-objection card is the one Parker ranted about; the rant-fix had cured the open-set but left a fuzzy 7-word cloze — now crisp.)
  - **Real leaks (R3):** helmet "fall zone" card had the source definition as a visible appositive (→ Back Extra Meaning line).
  - **Disallowed HTML → clean lists:** the ethical-checklist `<table>` and the 8-factor `<div>` list became `<br>` lists (ETHICS mnemonic preserved as a `Mnemonic:` line); the six-question and eight-question checklists had their whole-sentence clozes tightened to key phrases while staying one cohesive card.
  - **Pasted reference images:** the Star-of-Life and cyanide cards had a dead `<a href="google.com/url…"><img></a>` mobile-paste wrapper — unwrapped to keep the `<img>`, drop the inert `<a>`.
  - **Ch5 (medical terminology):** all 587 root↔meaning pairs re-verified independently (0 wrong; the angin/o fix holds). `-cele` corrected "tumor or swelling" → "hernia, protrusion, or swelling" (its own hydrocele/cystocele examples are herniations, and the deck's own `cel/o` already reads that) — flagged for Parker to reconcile with his course root sheet. The `prefix` concept card ("location and intensity") flagged, not changed (internally consistent).
  - **Numerics:** every dose/distance/threshold/time-window that survived (AEMT hours, rigor/putrefaction windows, CO 200x, bleach 1:10, CO2 >10-12%, VHF/UHF, 100 ft, check-in 10-20 min, …) was verified verbatim against a specific source page and marked `needs_human_check` per the safety overlay — content correct, human glance requested.
  *Systemic hardening (the real point — so this class can't recur or hide):*
  - **`check_cards.py` bloated-c2 / fuzzy-cloze detector (R12):** any cloze group with exactly ONE span hiding ≥9 words is now flagged. This is the mechanical catch the class lacked. Grouped lists (multi-span groups) are exempt, so cohesive lists stay whole. Editor check #6 and card-recipes §4 already named the rule; now the gate enforces it.
  - **`check_cards.py --live <N|all>`:** audits the cards as they actually exist in Anki, not just the pre-staging JSON. This is how the `<a>` anchor tags (hand-pasted on Parker's phone) were caught — the file gate had never seen them because it only ever checked JSON *before* staging. Relaxes the HTML gate like `--audit`; diagnostic only.
  - Plus an empty-cloze HARD error and a double-escaped-markup (`&lt;`/`&gt;`) warning.
  - **`test_regressions.py` 17 → 20 cases** (R12 bad / crisp-good / grouped-list-good); all green. The generous husk/eqhusk were left deliberately generous (the LLM judge clears visible-anchor contrasts and multi-line two-way cards — suppressing them mechanically risks a false negative on the class Parker rants about), and the committed first-letter-hint license was preserved.
  *Lesson (encoded):* "everything else confirmed sound" is only as strong as *what you looked for* and *what the gate can catch*. A whole defect class survived a 20-point human-agent pass because it was neither a sharp named check nor mechanically detectable. The durable fix is all three at once — **name it** (R12 rule + editor #6 wording), **mechanize it** (`check_cards.py`), and **audit the live deck** (`--live`), each with a regression test — which is what turns "I think it's clean" into "the class can't hide."

- **2026-07-19 (verification round) — an independent session adversarially tested the fresh Cold-Solve Gate and hardened it.** A second Claude session, asked by Parker to fix the same ambiguity complaints, found the batch already done (below) and took the verifier role instead of duplicating it. Outcome: (1) **a false-negative hole in the first-letter-hint detector** — it licensed letter hints whenever ANY acronym appeared in the stem, and "EMT/EMS/CPR" appear in half the deck's stems; licensing now requires the group's hint letters to actually spell into a stem token (contiguous run ≥2, or in-order subsequence ≥3 for Rx-gap mnemonics like CHART). The hardened detector re-swept all 899 live cards: caught the Ch4 SBAT card, whose `::T` hint leaked the final letter of the co-clozed acronym (fixed live + in canon: hint → `::component`), and correctly re-cleared everything else, including CHART. (2) **The regression library is now executable** — `scripts/test_regressions.py` runs the real checker over embedded BAD/GOOD exemplars for R2/R3/R7/R10/R10b/R11 (17 cases, both directions: missed-catch and over-flag); run it after any checker/rule change. (3) Re-verified live state independently: the 3 ranted cards fixed, feedback inbox empty, angin/o corrected, all five chapter canons re-stamped HARD-clean. Lesson encoded in regression-cases.md R11 ("Precision round"): a detector's licensing exemption is itself a card-rule surface — test the exemption, not just the catch.

- **2026-07-19 — the Cold-Solve Gate: three "impossible to answer cold" rants became three hard rules + mechanical checks + a full-deck audit.** Parker studied the decks and left `Card Feedback` complaints on three cards, all the same root grievance — *cards he could not answer on first sight even though he knew the material* ("I had no chance… an absolute set-up for failure"). Harvested via `feedback_harvest.py`. The three, and the distinct ambiguity class each exposed:
  1. **Ch1 medical-errors card — first-letter-hint copout (→ R11).** `{{c1::rules-based failure::r}} {{c1::knowledge-based failure::k}} {{c1::skills-based failure::s}}` — "your hint is a letter… that's literally giving away the answer, an easy copout." *Fix:* dropped the letter hints and exposed the shared structure — "a {{c1::rules}}-based, a {{c1::knowledge}}-based, or a {{c1::skills}}-based failure" — so recall is the crisp discriminator with no leak.
  2. **Ch2 religious-objection card — open-set cloze (→ R9).** `strongly oppose {{c1::medications, blood, and blood products::3 items}}` + `you must {{c2::report the objection to the next level of care}}` — "there are so many things that could fit in that hole… impossible to answer without having first seen the answer." *Fix:* made the opposed items VISIBLE (they were the open-set blank) and tested only the imperative duty, framed so exactly one action is forced ("respecting the refusal is not enough; you must also {{c1::report it to the next level of care::duty}}").
  3. **Ch3 governmental-immunity card — all-blanks-at-once husk (→ R10).** BOTH `governmental immunity` and `municipalities or other governmental entities` were hidden under the SAME `c1`, so each blank's only cue was the other, also hidden — "how am I supposed to guess both at the same time… I had no chance." *Fix:* split into `c1`/`c2` (two-way), so each resulting card reveals one span as the anchor and tests the other.
  *Systemic (all three, encoded as a permanent gate):* added **card-rules #16–18** (the Cold-Solve Gate — a knower must be able to produce the exact answer from the visible side alone, cold), **editor checks #18–20**, **parker-preferences** (the Cold-Solve rule, confirmed), and **regression cases R9/R10/R11** (each with the BAD card, the GOOD fix, and the GOOD cards the checks must NOT over-flag: two-way defs, cohesive grouped lists, forced-choice binaries, real spelled-mnemonic first-letter hints). Extended **`check_cards.py`** with two new mechanical warnings — a first-letter-hint leak detector (fires when a hint is the leading letter(s) of its own answer and no spelled acronym is in the stem; licensed for SAMPLE/DCAP-BTLS) and an all-blanks-at-once husk detector (fires on exactly two mutually-dependent multi-word spans under one number joined by real templating; excludes coordinate pairs, `<br>`-lists, counted sets, and forced-choice co-clozes). Validated: the checker catches all three rant cards and does not over-flag the calibration GOOD cards. Open-set (R9) has no reliable mechanical proxy and is enforced by the LLM judge + the cold-solve test.
  *Then a full-deck refinement audit (Parker: "audit every single one, chapters 1-5, make them pitch perfect — iron being refined"):* re-audited all ~899 live cards against the sharpened 20-check standard with an agent fleet (one per chapter for 1-4; six shards for Chapter 5's 587 word-root cards, whose two-way root↔meaning bindings and clinical-example HTML + TTS audio were preserved as correct-by-design). The sharpened checker also caught a live in-the-wild repeat of the same class before the audit even ran: the Ch3 negligence-elements card carried `::D ::B ::D ::C` first-letter hints (Duty/Breach/Damages/Causation).
  *Audit result — 899 live cards swept, 14 refined, everything else confirmed sound:*
  - **Ch1 (32):** 28 pass, 4 fixed — a patient-advocate leak (appositive paraphrased the answer), the off-line/online **synonym-equation husk** (all four synonym terms under one `c1`), an AEMT-hours numeric flag + stem smoothing, and an AEMT-level husk.
  - **Ch2 (89):** 87 pass, 2 fixed — a power-lines **R10 husk** (four attributes under one `c1`) and a family-notification **R9 open-set** whole-sentence cloze.
  - **Ch3 (84):** 79 pass, 2 fixed — the negligence-elements **R11 letter-hint copout**, and (caught on re-verify by the new synonym detector, missed by the agent that ran before it existed) the Expressed = actual consent **synonym husk**; plus 4 numeric-only flags.
  - **Ch4 (107):** 102 pass, 2 content-fixed — the stethoscope card (three spans co-clozed → crisp `c1`/`c2`) and the verbal-report husk; plus 3 numeric-only flags. The Reflection/Interpretation mirror and the VHF/UHF paired-contrast were confirmed sound, not husks.
  - **Ch5 (587):** 586 pass, 1 fixed — the long-suspected **angin/o mis-key** ("blood vessel" → "choking, to choke"; that gloss belongs to the separate angi/o card), confirmed against the neighbor card and its own angina clinical examples. The two-way root↔meaning bindings, the clinical-example HTML, and the TTS audio were all preserved as correct-by-design.
  *Two checker refinements the audit itself drove:* (1) a **synonym-equation husk** detector (`equation_husk_groups`, regression **R10b**) after the Ch1 audit found the off-line/online husk the word-count proxy structurally could not see — deliberately generous (over-flags visibly-anchored acronym cards like POLST/MOLST and grouped two-way defs like SOAP, which the LLM/human clears, because missing a real synonym husk is the costly error); (2) an **`--audit` mode** for `check_cards.py` that skips the minimal-HTML gate when verifying pre-existing cards (which legitimately carry rich reference images, formatted tables, and the Ch5 clinical-ex blocks) while keeping every meaningful check — the strict `<b>/<i>/<br>/<img>` gate still governs newly generated cards. All fixes were written to the live Anki notes and mirrored into the `work/chapter_N_cards.json` canon (which now also re-derives `needs_human_check` from the numeric detector on every resync); all five chapters re-verified HARD-clean and re-stamped.

- **2026-07-02 — Back Extra components now separated by a paragraph break (`<br><br>`), not a single `<br>`.** Parker showed before/after screenshots: he wants real white space between distinct labeled components (a `Distinguish:` line and a `Pitfall:` line) so his eye can bounce between them; a single line break packed them too tight. Purely aesthetic, but he cares. *Best-way implementation (rule + mechanical harness):* documented the preference in `parker-preferences.md`, `card-rules.md` #5, and `note-format.md`; AND added `paragraphize()` to `anki_write.py`, which collapses any run of `<br>` in Back Extra to exactly `<br><br>` at write time (idempotent — cards already authored with `<br><br>` are untouched) so the spacing is guaranteed even if a card is drafted with single breaks. Back Extra never breaks inside one flowing component (rule #5), so every `<br>` run is safely a component boundary. *Applied to the whole EMT section:* normalized 211 live cards (0 single-`<br>` multi-component remaining) and the `work/*_cards.json` sources.

- **2026-07-02 — flat dumping deck + `claude_generated` tag replaced with a per-chapter structure.** After the Chapter 5 run (587 notes / 1,151 cards), everything was piling into one `all::EMT::_Review` deck — it had ballooned to ~1,488 cards and Parker couldn't tell chapters apart or track what he'd reviewed. He designed a new structure (built the Chapter 1 example himself) and asked to roll it out everywhere. *Change (system, not a card bug):* (1) per chapter, cards now stage into `all::EMT::Chapter <N>::claude review`; Parker PROMOTES keepers into the sibling `all::EMT::Chapter <N>::Book Highlights` deck himself after his first pass (deck names are case-exact: `Chapter <N>`/`Book Highlights` Title Case, `claude review` lowercase). (2) The `claude_generated` tag was removed from every existing card (794 notes) and from the writer — cards now carry `ch<N>` ONLY (Parker likes the chapter tag, found the other one noise). *Migration done live:* moved all 1,488 cards from `_Review` into the right chapter's `claude review` by chapter tag, stripped the tag, deleted the empty `_Review`. *Bury-siblings re-fix:* the fresh subdecks defaulted to Anki's `Default` preset (bury OFF), which would break two-way-definition spacing (see 2026-07-01 below); rebuilt the `EMT` preset (bury new/rev/interday ON, cloned from Default so other decks stay untouched) and assigned it to `all::EMT` + all 8 subdecks. *Skill updated:* `anki_write.py` (derives the deck per card from its `chapter` field, auto-creates both subdecks AND copies the `all::EMT` preset onto them so future chapters stay bury-on, tags `ch<N>` only), `note-format.md`, `parker-preferences.md`, `SKILL.md`, `README.md`, `editor-checklist.md`.

- **2026-07-01 — Chapters 2 & 3 test run (89 + 85 cards) surfaced 4 systemic issues, all fixed as rules + tests.** Ran the full pipeline on Ch2 (86 highlights) and Ch3 (73), then four independent reviewers (Ch2 adversarial, Ch3 adversarial, a numeric safety audit against the actual PDF pages, a margin-comment/taste audit).
  1. **List undercount across a page break (the one real accuracy bug).** Ch3 "decision-making capacity" said "7 factors" but the book lists 8 — the 8th was on the next page, past the 450-char context window, on a card Parker had marked "Know all of these!!". *Systemic:* extractor now pulls the next page for any `list_lead_in` highlight + tags the flag; `check_cards.py` warns on stated-count > clozed-items; card-rules #14 + editor #17 + regression **R7**. Card corrected to 8.
  2. **Cross-card give-away.** A Ch2 "classify the contact route" scenario reused the *exact* examples its definition siblings gave — answerable by pattern-matching. *Systemic:* card-rules #13 + editor #16 + regression **R6**; fixed with fresh exemplars.
  3. **Scenario→action fuzzy clozes.** Several Ch2 vignettes hid a whole sentence. *Systemic:* card-rules #15 + regression **R8**; fixed to crisp keyword clozes.
  4. **Margin-comment intent not surfaced at hand-off.** *Systemic:* SKILL Stage 1 + Stage 4 + parker-preferences now require answering margin questions and listing "look into this" items. (Also fixed: a Back-Extra-restates-Text card and a weak c3 cloze.)
  Safety audit verdict: **every dose/distance/temperature/ratio/time-window matches the textbook exactly** — the only content error was the 7-vs-8 count. Both chapters re-verified clean and stamped.

- **2026-07-01 — staging deck path was wrong (`EMT::_Review` vs `all::EMT::_Review`).** The 2026-06-29 reorg put his whole collection under an `all::` parent, but `anki_write.py` still wrote to a bare `EMT::_Review`, which Anki created as a NEW stray top-level deck instead of his real staging deck. Caught when the post-write count didn't match. *Fix:* moved the 174 staged notes into `all::EMT::_Review`, deleted the empty stray, and pointed `DEFAULT_DECK` (+ `note-format.md`) at the full `all::EMT::_Review` path.

- **2026-07-01 — the writer targeted a DELETED note type (would hard-fail on the next run).** Two hours after this skill was built (2026-06-29), Parker restyled his whole collection to the AnKing look; his homemade cloze cards migrated to the **`AnKing Cloze`** note type and the old **`01_Cloze - Parkers Note Type`** was deleted. But `anki_write.py` still hard-coded the old model (and `sys.exit`s if it's missing), so the very next `anki_write.py` run would have died before writing a card. *Systemic (silent config drift):* pointed `MODEL` at `AnKing Cloze` (verified live via `notesInfo` on `tag:claude_generated` — his 33 EMT notes are on it), documented the 5-field layout in `note-format.md` (we fill only `Text` + `Back Extra`; the other three are his study fields — never write them), and made the "model missing" error message tell a future session how to re-point it. Dry-run verified against the live collection.
- **2026-07-01 — bury-siblings was never turned on (the two-way-definition freebie).** Two-way definition notes make two sibling cards; without bury-siblings they can appear back-to-back the same day, which inflates the sense of mastery. The 2026-06-29 adoption of two-way defs had flagged this as a to-do and it was never done. *Config fix:* created a dedicated **`EMT` deck preset** (cloned from Default so his other decks are untouched) with new/review/interday bury all ON, and assigned it to `all::EMT` + `all::EMT::_Review`. Verified live.

- **2026-06-29 — "the pathway card's parentheticals give away the answer."** The `(state authority granted)` after `{{c1::licensure}}` was the term's definition → a crutch. *Systemic (leak/crutch class):* fixed the card (defs → Back Extra), encoded `card-rules.md` #3 + `editor-checklist.md` #15 + regression case **R3**, and added the parenthetical-after-cloze + literal-answer-in-stem warnings to `check_cards.py`. Also caught 2 more leaks (ADA Title I, EMR) in an audit.
- **2026-06-29 — "the public-health card doesn't test the goal."** The goal ("preventing health problems") sat as visible scenery, untested. *Systemic (under-clozing):* fixed to a 3-cloze card, sharpened `card-rules.md` #7 + `editor-checklist.md` #4, regression case **R1**.
- **2026-06-29 — "the AEMT skills are sitting unclozed."** Used the "such as" rule as an excuse to skip a real skill list. *Systemic (shortcut / under-clozing):* added the explicit Fact Pass (must-test / supporting / skip) to the pipeline; regression cases **R1/R5**.
- **2026-06-29 — over-production (76 cards from 36 highlights).** The decomposed per-unit fan-out duplicated facts across units. *Systemic:* added the Stage 2.5 global-consolidation pass + `card-rules.md` #12; regression case **R2**.
