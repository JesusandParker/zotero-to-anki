# Python caches
__pycache__/
*.pyc

# macOS
.DS_Store

# ephemeral verification stamps (regenerated by check_cards.py)
*.verified

# skill venv (PyMuPDF for figure extraction) — recreate with:
#   python3 -m venv .venv && .venv/bin/pip install PyMuPDF pillow
.venv/
