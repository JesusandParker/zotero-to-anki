# D12 notes — Neurologic assessment · pupils · PEARRL (idx 168–179, p1050–1055)

16 notes from 12 marks (1.33/mark — slightly above the 0.6–1.2 guide; justified by two
5-item lead-in lists, a 6-item trigger list, a 6-letter mnemonic, and the chapter's #1
margin-comment card, all of which rule 23 chunking expands by design).

Renders read in full: p1050, p1052, p1054, p1055 (all at 170 dpi; p1052/1054/1055
rendered this session). Every card's claim was checked against the corresponding render;
`verified_against`/`verified_by: "agent"` recorded on all 16. No numeric facts exist in
these 12 marks (no doses, thresholds, or clinical values — GCS bands and MAP > 65 are
context-only and unmarked), so `numeric: false` everywhere.

---

## HOW THE MARGIN COMMENT (idx 175) WAS HONORED — the #1 card of the chapter

Parker's comment (voice-garbled "Akio motor" = oculomotor): *"I wanna know that it's the
[oculo]motor nerves that do this that's the piece I wanna memorize."*

Read verbatim off the p1054 render: *"Pupil size is regulated by a series of continuous
motor commands that the brain automatically sends through the oculomotor nerves to each
eye, causing both pupils to constrict to the same appropriate size."*

Honored exactly as the brief specified, twice:

1. **Card AR4 (the requested card):** clozes **only "oculomotor"** — the marked span minus
   "nerves," which stays visible as the rule-18 scaffold — with the full constriction
   behavior (brain → motor commands → each eye → both pupils constrict to the same size)
   visible as the cue. Hint `::cranial nerve` is a pure form-label (prevents the degenerate
   echo-answer "motor nerves" without leaking content). Back Extra adds the sensory-in /
   motor-out mechanism (grounded in the same paragraph) plus a word-parts cue
   (*oculo-* eye + *motor* command) aimed directly at his stated goal: memorizing the name.
2. **Card AR5 (auto-pair vignette):** the brief invited an auto-pair "if the content
   supports it" — it does (the consensual response in idx 173/175's shared paragraph). A
   fresh penlight scenario (light in the right eye, left pupil constricts) forces the same
   answer, "oculomotor," from the field-facing direction. This pairs the comment card with
   idx 173 exactly as the brief described.

Cold-solve check on AR4: with the cloze covered, the stem constrains to a nerve name
carrying motor commands to the eyes that constrict pupils — forced for a knower,
undecodable for a non-knower. One-way (his comment asks for exactly one direction).

---

## Block AQ_neuro_when (idx 168) — 3 notes

Fact pass:

| proposition | where | class | disposition |
|---|---|---|---|
| Perform a neuro assessment anytime the patient has: changes in mental status, possible head injury, stupor, dizziness, drowsiness, or syncope (6 triggers) | idx 168 highlight; p1050 render verbatim | MUST | AQ1 + AQ2 (chunked 4+2) |
| The assessment can begin hands-off (talking during primary assessment) | 168 context | SUPPORTING | AQ1 Back Extra `Why:` |
| Evaluate LOC/orientation, AVPU, speech | 168 context, unmarked | SKIP (unmarked) | not carded |
| MAP > 65 needed to perfuse vital organs | 168 context (Words of Wisdom box), unmarked | SKIP (unmarked) | see flag below |

- **Rule #14 count:** full trigger list read off the p1050 render — exactly **6** items,
  none spilling past the sentence; matches the extraction. Both notes print "six trigger
  presentations" as the part-whole anchor.
- **Load (rule 23):** 6 uncued members, no spelled mnemonic, no regenerating structure →
  chunked into two notes: AQ1 "four ongoing changes in how the patient feels or responds"
  (changes in mental status, stupor, drowsiness, dizziness — 4 uncued, at cap) + AQ2 "two
  discrete events" (possible head injury, syncope). Partition names are mine → **printed,
  never clozed** (rule 23.1); unordered set → **no anchor note**; `Roster:` on both with
  own members bold.
- **Archetype:** §6 grouped list (chunked) + §9 application. AQ3 is the "when do you do X"
  auto-pair: a recovered-syncope vignette forcing "neurologic assessment" — the trigger
  list's field direction (presentation → assessment indicated), one stem, one cloze.
- Judgment call: group names were chosen so no label restates a member (checked "ongoing
  changes in how the patient feels or responds" against "changes in mental status" — a
  semantic cover in the radio-report-precedent style, not a lexical restatement).
- Note for the checker/judge: AQ1/AQ2 each print "six" (set size) while clozing 4 and 2 —
  that is the sanctioned chunk-position framing, not an undercount; the Roster carries all
  six on both notes.

## Block AR_pupils (idx 169–176) — 6 notes

Fact pass:

| proposition | where | class | disposition |
|---|---|---|---|
| Pupil diameter + light reactivity reflect the brain's perfusion, oxygenation, condition | idx 169; p1052 | MUST | AR1 (cloze "brain", `::organ`) |
| Normal room light → about midsize | idx 170; p1052 | MUST | AR2 row 1 |
| Less light → dilate (to let more light in) | idx 171; p1052 | MUST | AR2 row 2 (the "why" moved to Back Extra — visible it would decode "dilate") |
| Bright light near the eye → constrict | idx 172; p1052 | MUST | AR2 row 3 |
| No light → fully relaxed and dilated | idx 174; p1054 | MUST | AR2 row 4 |
| Light in ONE eye → BOTH pupils constrict equally | idx 173; p1052 | MUST | AR3 (forced-choice hints) + AR5 vignette |
| Oculomotor nerves carry the constriction commands | idx 175 + comment; p1054 | MUST (Parker's #1) | AR4 + AR5 — see section above |
| Anisocoria = normally unequal pupils, small number of patients | idx 176; p1054 | MUST | AR6 two-way definition |
| Pupil size normally changes instantly | 174/175 context, unmarked | SUPPORTING | AR2 Back Extra |
| Eye surgery/other conditions → normally non-reactive pupils | 174/175/176 context, unmarked | SUPPORTING | AR2 + AS1 Back Extra `Pitfall:` |
| GCS score bands (13–15 / 9–12 / ≤8) | 169/170 context, unmarked | SKIP (unmarked; SHARED explicitly forbids) | not carded |

- **The panel (AR2):** per the brief, idx 170+171+172+174 became ONE compact keyed panel
  (light condition → response, 4 rows, word answers — not a value column, so rule 25 does
  not apply). Every direction blank carries a forced-choice hint (`::dilate/constrict`),
  per the brief's #22/recipes-§8 instruction; the midsize row gets the slot-label `::size`.
  Rows laid out with `<br><br>` (rule 19). Row labels checked against rule 20 — each names
  the light condition, none restates its answer.
- **AR3 (consensual):** kept as its own card per the brief. The book's tail ("…to the
  appropriate size for the pupil receiving the most light") was moved to the Back Extra —
  left visible it decodes "both" (it presupposes two pupils under different light).
  Judgment call: the physiology label "consensual response" appears nowhere in the AAOS
  text, so it was deliberately NOT imported onto any card (rule 10); the brief used it only
  as shorthand.
- **AR6 (anisocoria):** two-way definition (term↔meaning, the house default); the
  discriminator the brief named — *normally* unequal, a normal variant — is inside the c2
  answer. `Pitfall:` carries the unmarked presume-sentence (fully inside idx 176's own
  context, so licensed); `Parts:` decomposes the term (an- + iso- + cor-).
- **AR1:** clozes "the brain" (the assessment-significance direction an exam asks:
  pupillary checks report on which organ). Back Extra's "set from inside the skull" line
  deliberately avoids naming the oculomotor nerves so AR4's answer is not re-shown on a
  sibling's back; grounded via idx 175's context, hence `from_idx: [169, 175]`.

## Block AS_pearrl (idx 177–179) — 7 notes

Fact pass:

| proposition | where | class | disposition |
|---|---|---|---|
| 5 abnormal pupil reactions: fixed / dilate-with-light / sluggish / unequal / unequal-on-light-change | idx 177 (lead_in); p1054 render | MUST | AS1 (3 reaction failures) + AS2 (the 2 inequality findings) |
| Unconfirmed abnormal pupillary response → presume altered brain function (CNS depression or injury) | 176/177 context, unmarked framing sentence | MUST-adjacent | visible framing on AS1/AS2 + the decision forced in AS7 (see judgment call) |
| 5 causes of depressed brain function: brain/brainstem injury · trauma or stroke · brain tumor · inadequate oxygenation/perfusion · drugs or toxins (CNS depressants) | idx 178 (lead_in, PARTIAL); p1055 render | MUST | AS3 (3 structural) + AS4 (2 nonstructural) |
| PEARRL expansion: Pupils Equal And Round Regular in size React to Light | idx 178 highlight tail; p1055 render | MUST | AS5 (letter-hinted grouped reveal — spelled-mnemonic license) |
| Normal pupils → report "Pupils = PEARRL"; abnormal → describe in full | idx 179; p1055 render | MUST | AS6 |

- **Rule #14 counts + the interleaved-letters trap (both read off renders, recorded in
  `visual_source`):**
  - Abnormal reactions (idx 177): p1054 prints exactly **5** bulleted items; the highlight
    matches all five; nothing continues onto p1055.
  - Causes (idx 178): the lead-in sits at the bottom of p1054 and the bulleted list at the
    top of p1055 — exactly **5** items. The extraction's "…include the following: P E A R
    R L Injury of the brain…" interleaves the **PEARRL sidebar letter-table** into the
    causes list; the render shows the letters belong to the separate mnemonic block. The
    two lists were separated accordingly, which also explains the mark's PARTIAL grounding
    — resolved by the render read, so no `flag_for_parker` needed.
- **Load/shape for the abnormal-reactions list:** 5 uncued long phrases, no mnemonic →
  chunked. AS1 groups the three *reaction* failures (none / backward / slow — a genuinely
  derivable trio, each clozed whole with its crisp discriminator leading). AS2 reshapes
  the two near-duplicate inequality bullets into one note that hides the finding
  ("unequal in size") and the distinct condition ("when a bright light is introduced into
  or removed from one eye") — the honest recall, instead of two rows differing by one
  clause. Cued-rows was tried and rejected: every natural key ("no movement → fixed")
  restates its answer (rule 20). Rosters on both notes carry all five, own members bold.
- **Open-list handling (AS3/AS4):** the book hedges — "SOME of the causes… include" — and
  parker-preferences forbids carding open-ended lists as closed. Middle path taken: the
  stems keep the faithful "include," the chunk frames (three *structural* / two
  *nonstructural*, my printed labels, never clozed) make each retrieval bounded, and AS3's
  `Cue:` states the openness outright. No "the five causes are" overclaim anywhere.
- **PEARRL (AS5/AS6):** spelled mnemonic → first-letter hints licensed and 6 rows under
  one number licensed (rule 23's mnemonic handle); expansion card was explicitly licensed
  by the brief. The A genuinely stands for "And" (verified on the render — clozed as
  such). AS6 carries idx 179's real discriminator: PEARRL is the report form for NORMAL
  pupils only, with the book's own long-form abnormal example quoted in `Ex:`.
- **AS7 (auto-pair vignette) judgment call, flagged for the judge:** the decision it
  forces — presume **altered brain function (CNS depression or injury)** — is the unmarked
  framing sentence, not a marked span. License: idx 177's mark opens "Specifically,
  evaluate…", whose whole meaning hangs on that immediately-preceding sentence (Rule-0
  zoom-out), the sentence sits verbatim inside BOTH cited marks' contexts (177 and 176),
  and the EMT auto-pair rule requires a vignette forcing the decision that follows from
  the marked findings. The findings shown in the stem (sluggish + unequal) are a fresh
  composite scenario, not any sibling's `Ex:` exemplar (rule 13 checked).

---

## Flags / one-liners for Parker (unmarked but exam-relevant, per contract)

- **MAP > 65 mm Hg** (Words of Wisdom box, p1050) sits unmarked inside idx 168's context.
  Not carded. It may belong to a neighboring unit's indices; if not, it looks exam-worthy.
- **GCS severity bands** (13–15 mild / 9–12 moderate / ≤8 severe, p1052) are unmarked and
  explicitly off-limits per SHARED — not carded, noted only for completeness.
- No `flag_for_parker` cards: no thin/uncardable spans remained after Rule-0 grouping, and
  idx 178's PARTIAL grounding was fully resolved by the render read.

## Checker adjudication (self-edit pass)

`check_cards.py` was run on the unit file (stamped OK, 16 cards, grounding checked
against `chapter_10_highlights.json`). Two warnings raised, both adjudicated:

- **`fragment_clozed_list` on AR2 (the light panel) — cleared, stands.** The detector's
  geometry (rows not leading with their cloze, hiding fewer words than shown) matches the
  recipes-§8 direction-of-change keyed panel exactly — each row hides the ENTIRE answer
  (the pupil response), cued by its visible light condition; nothing is a filler word in a
  reproducible frame (rule 22's "not a violation" clause). Clearance recorded in the
  card's `verified_by`, naming the detector, per note-format.md.
- **`row_label_tautology` on AS2 — fixed.** The frame shared the token "light" with the
  hidden condition; reworded to "not only about how the pupils react," removing the
  shared token while staying faithful. Warning no longer fires.

## Verification record

| render | what was verified on it |
|---|---|
| p1050 | 6-item neuro-assessment trigger list, verbatim (AQ1–AQ3) |
| p1052 | brain-status sentence; midsize / dilate / constrict rows; consensual sentence (AR1–AR3, AR2 rows 1–3) |
| p1054 | oculomotor sentence verbatim (AR4/AR5 — the margin-comment card); no-light row (AR2 row 4); anisocoria sentence (AR6); 5-item abnormal-reactions list + its count (AS1/AS2/AS7); causes lead-in position |
| p1055 | 5-item causes list + count, separated from the interleaved P E A R R L sidebar letters (AS3/AS4); PEARRL letter table incl. "A = And" (AS5); normal-report + long-form sentences (AS6) |

`verified_by: "agent"` on all 16 cards; `visual_source` set on the six AS-block cards
whose counts/list-separation genuinely came off renders (177/178/179 material), null on
cards whose facts sit verbatim in EXACT-grounded extraction text.

---

## Editor pass (independent adversarial, 2026-08-29)

Every check in `editor-checklist.md` run on every card, cold-solve PER ROW; all four
renders re-read independently and every count re-verified against the page (6 triggers
p1050 · 4 light rows p1052/p1054 · 5 abnormal reactions p1054 · 5 causes p1055 · PEARRL
letters incl. "A = And" p1055). Confirmed idx 178's interleaved "P E A R R L" letters are
the sidebar mnemonic table, correctly separated from the causes list — the causes list on
p1055 is exactly the 5 items the cards test.

**Verdicts: 11 PASS · 5 REWRITE · 0 DROP.** All 12 marks still covered; no fact dropped.

Rewrites (applied in place):

1. **AQ1 — partition label leaked toward a member (checks 15/22-spirit).** "Four are
   ongoing changes in how the patient feels or responds" repeats "changes" verbatim and
   glosses "mental status" ("how the patient feels or responds" is close to its
   definition), so a non-knower could half-decode member 1. Now "Four are ongoing
   states:" — same states-vs-events partition, zero gloss. (Same standard the self-edit
   applied to AS2's "light" token.) AQ2's "discrete events" label was checked and is
   clean — PASS.
2. **AR2 row 4 — hint didn't match the answer's spelling (recipes §8).** Answer "fully
   relaxed and dilated" carried `::dilate/constrict`; now `::dilated/constricted`.
   Rows 2–3 (`dilate`/`constrict` verb answers) already matched — untouched. The
   standing `fragment_clozed_list` clearance in `verified_by` re-examined and upheld:
   §8 keyed direction panel, each row hides the ENTIRE answer cued by its light
   condition. Still the only checker warning; no new ones.
3. **AS1 — missing whole-set anchor (rule 23.1 position-printing; part-and-whole
   preference).** Lead now "Of the five abnormal pupil findings that indicate altered
   brain function, three are ways the <i>reaction</i> to light goes wrong:" — the
   five-count skeleton is printed the way AQ1/AQ2 print "six." Token-share audit: lead
   shares only the family axis words ("reaction," "light") that name the partition, not
   any row's discriminator (fixed/backward/sluggish).
4. **AS2 — same anchor added**, standalone-safe phrasing: "Two of the five abnormal
   pupil findings are not about how the pupils <i>react</i> — …" The earlier
   `row_label_tautology` fix (no "light" token in the frame) preserved; warning still
   does not fire.
5. **AS6 — under-clozing (check 4).** The abnormal-side rule ("describe using the longer
   form") sat visible as scenery while only "PEARRL" was tested. Added
   `{{c2::described using the longer form}}` (book's own wording, p1055) — hintless,
   anchored by the visible "single mnemonic … instead" contrast (rule 21 shape b). Two
   cards now: name the mnemonic (c1) / recall the abnormal-report rule (c2). The Ex:
   long-form quote becomes c2's worked example. Not a sibling-split: c1/c2 are two
   different facts, the sanctioned two-way shape.

**How the oculomotor pair (AR4/AR5 — Parker's margin request) held up: PASS, both,
unchanged.** Tried hard to break them:

- *Forcing:* with "oculomotor" covered, both stems constrain to one cranial nerve
  carrying motor commands brain→eye for pupil constriction. One answer; no competitor
  survives ("optic" is killed by the visible "motor commands" — which is exactly why
  that phrase must STAY visible).
- *The morphological echo, adjudicated:* the stems contain "motor" and "eye," the
  answer's own morphemes. Considered stripping "motor" and rejected it: (a) it is the
  book's load-bearing word and the sensory-vs-motor discriminator that blocks the most
  tempting wrong answer (optic); (b) exploiting the echo requires outside etymology
  (oculo- = eye), i.e., partial real knowledge, not decoding of THIS card; (c) this is
  rule 18's endorsed shape — shared structure ("…motor", "nerves") visible, the
  discriminating morpheme recalled and bound. The `::cranial nerve` hint is a clean
  form label that does real work: it blocks the degenerate echo-answer "motor nerves."
- *AR5 vs check 16:* its answer "oculomotor" is visible on NO sibling (AR4 clozes it
  too; AR1's back deliberately avoids naming it — confirmed). Exemplar (penlight right
  eye → left constricts) is fresh, appears in no sibling's Ex:. AR4/AR5 both cloze the
  same word by design — the profile's auto-pair license (recall direction + field
  direction), invited by the brief for the chapter's #1 requested fact.

**Known, accepted cross-card exposures (documented, not defects) — for the consolidator:**

- AR3's answers ("both"/"equally") appear as visible context on AR4/AR5 — unavoidable:
  the brief mandates the constriction behavior stay visible as AR4's cue, and the
  consensual response IS AR5's forcing situation. AR3 still earns its keep as the only
  recall-direction test of idx 173.
- AS7's answer ("altered brain function") is printed as framing on AS1/AS2 leads (and
  paraphrased on AS3/AS4) — it is the book's organizing sentence for the whole family;
  AS7's real test is the presume-under-no-confirmation decision, which no sibling
  gives away. Fresh composite exemplar, checked.
- AS6's c1 answer "PEARRL" is visible on AS5's stem — the recipes' own sanctioned
  §6+§11 mnemonic pair (identical to the SAMPLE worked examples).

Checker re-run after edits: **16 cards, 0 HARD, 1 warning (the upheld AR2 clearance),
stamped OK.** Open-set hedge on AS3/AS4 re-verified as honest ("include" kept; Cue:
declares the open set). No numeric answers anywhere; idx 178's PARTIAL grounding stands
resolved by the render read (verify_report will derive the flag from
`verified_against`/`verified_by`).
