# Unit F — Adolescent & Middle Adult: editor verdicts (marks 37, 38, 39)

Adversarial pass over the 5 drafted notes. Result: **2 PASS · 3 REWRITE (2 of them merged into 1) · 0 DROP**.
Output is now **4 notes → 8 Anki cards** (same card count, one fewer note).
`check_cards.py --highlights work/emt/chapter_7_highlights.json` → clean, stamped, exit 0.

---

## Verdicts (one line per drafted note)

1. **Growth-spurt sequence** (`hands and feet → long bones → torso`, c2 = 2–3 years) — **PASS**. Arrows sit OUTSIDE the clozes, all three steps share `c1`, lead-in colon + `<br><br>` matches recipes §7 exactly; the `c2` duration card keeps the sequence visible as its anchor and the `c1` card shows the count of blanks at a glance. Triage of the surrounding prose is correct: "2 to 3 years" was rightly clozed (it was visible scenery otherwise → R1), and "muscle mass and bone density have nearly reached adult levels" is **not** R1 because it never sits visible in the Text — Back Extra `Why:` is the right home for it. Numeric flags correct.
2. **Girls 16 / boys 18 / boys taller** — **PASS**. Not an R10 husk: the ENTITY anchors (girls, boys) stay visible and only the differing VALUES hide under one number — the multi-dimensional contrast R10 tells the judge to clear, and recipes §8's default. The binary `taller` carries its mandatory forced-choice legend `::taller or shorter` (check 13). Grounded verbatim; numeric flags correct.
3. **Menarche two-way definition** — **REWRITE (merged with note 4)**. The definition itself was sound (c2 side = 4 words, R12 clean), but it shipped as a bare definition while a must-test fact from the *same highlighted sentence* lived on a broken sibling. Merged into one 3-cloze note; Back Extra retuned (`Pitfall:` promoted per the priority order, `Why:` made symmetric — the source gives sperm AND oocytes, the draft gave only oocytes), and the secondary-sex list relabelled `Cue:` → `Ex:` (it is a set of examples, not a memory hook). `from_idx` widened to `[37, 38]` because the Pitfall now leans on the 12-year band, which lives in mark 37's context.
4. **"…beginning to menstruate before adolescence is `{{c1::not uncommon::frequency}}`"** — **REWRITE (folded into note 3 as `c3`)**. This was the unit's worst card and failed in both directions at once. (a) **Self-answering (R15):** the stem's own `but` — "usually an adolescent milestone, **but** beginning before adolescence is ___" — telegraphs that the answer is on the *it-happens* side, so a non-knower decodes it. (b) **Not cold-solvable (R9/R12):** with only a `::frequency` form-label, *common / uncommon / not uncommon / rare / unusual* all fit, so a knower cannot produce the exact fill either. The drafter's stated reason for splitting it off was **half right**: a `c3` phrased "…yet it is not uncommon for a girl to begin **menstruating** earlier" genuinely does leak `c2` ("the first menstrual bleeding"). But the fix for a leaky tail is to reword the tail, not to spawn a standalone freebie. The tail now refers to menarche obliquely ("reaching **it**", same sentence, so no dangling-referent violation) and contains no form of *menstruate*, killing the leak; the contrastive `but/though` is replaced by a neutral `and`, killing the telegraph; and the blank gets a true forced-choice hint `::uncommon or not uncommon` (check 13). Net effect: the card now runs *against* intuition — "the adolescent milestone" biases a guesser toward "uncommon", and the answer is the opposite — which is what makes it worth reviewing.
5. **Leading cause of death** — **REWRITE (c2 direction was still open)**. The drafter's `c1`/`c2` choice over recipes §8's same-number default is **correct and stays** — under one number the front reads "under 44 → ___ / 45–64 → ___" and each age band alone is a weak cue. Verified both directions: **c1 is forced** (given cancer at 45–64, "unintentional injury" is the only fill a knower produces). **c2 was NOT** — "under 44 → unintentional injury; 45–64 → ___" leaves *heart disease* as an entirely reasonable answer for a knowledgeable premed, and nothing visible discriminated. Fixed with card-rules #21(b)'s blessed remedy, a visible contrast naming the rejected alternative: `→ {{c2::cancer}}, not heart disease`. Grounded by entailment (if cancer leads, heart disease does not) and it leaks nothing — the answer's own definition never appears. Back Extra also rebuilt: the old `Distinguish:` line just restated the Text ("injury below, cancer above" → check 11), replaced by a real field edge (`Pitfall:` middle adults carry undiagnosed diabetes/hypertension, so a denied history does not rule them out).

---

## Checks run and what they found

- **Per-row cold-solve (checks 18–24) on every row of notes 1 and 5** — the two row-shaped cards. No row-label tautology (both leading-cause rows are age-band → cause, no shared stem), no self-answering row, no `Miscellaneous`-style empty label.
- **R1 (under-clozing), unit-wide** — every fact visible in any Text is tested. Facts left in Back Extra are ones that never appear in a Text, which is not R1.
- **R3 / R15 leak sweep** — the one real leak (note 4's `but`) is fixed; the two-way definition in note 3 is the sanctioned non-leak and was left alone.
- **R12** — every single-span blank is a crisp term/number; the two-way c2 side is 4 words.
- **R14** — notes 1 and 5 already used `<br><br>`; note 1's arrow chain correctly stays on ONE line (recipes §7), and a colon lead-in over a single row is explicitly not a packed list.
- **R2** — the "milestone" near-duplicate the drafter flagged for consolidation is gone; notes 3 and 4 are now one note.
- **Numeric/safety** — all four notes carry `numeric: true` + `needs_human_check: true`. Note 3 has no digit in its Text but its Back Extra asserts the age-12 boundary, so it is flagged with the rest.

## For consolidation

1. **Un-carded fact worth a deliberate call: "empty nest syndrome."** Mark 39's context defines it ("the need to adjust after their adult children leave the home (ie, empty nest syndrome)") — a clean two-way definition candidate sitting in my grounding pool. I did **not** card it: it is un-highlighted, it is psychosocial rather than the mortality fact Parker marked, and another unit may hold a p717 psychosocial mark that would duplicate it. If no other unit covers it, it is the single best addition available from these three marks.
2. **The drafter's unit-B contradiction flag stands.** Mark 6 (p688) says the leading cause of death for neonates/infants is congenital abnormalities, which reads as a conflict beside my "younger than 44 → unintentional injury." Still not assertable from my marks; if unit B cards it, add a cross-linking `Distinguish:` to one side.
3. **`from_idx` on note 3 is `[37, 38]`**, not `[38]` alone — deliberate, because its Back Extra uses the 12-to-18 band from mark 37's context. Both marks are p708, so page-level provenance and figure matching are unaffected.
4. **"12 to 18 years" now appears in two Back Extras** (notes 2 and 3). If unit A produces a card whose *answer* is "12 to 18 years", these are Back Extra lines shown only after answering, so there is no cross-card give-away — but it is the one place this unit touches another's answer space.
5. **No figure needed or wanted.** None of marks 37–39 is `needs_visual`; no card carries `visual_source`. The p708 FIGURE 7-8 adolescent photo is a stock portrait carrying no information — if the matcher proposes it, that is the "congruent but empty" case for the judge to decide, and nothing here depends on it.
6. **The `.verified` stamp** next to this file is keyed to this file's hash and will not survive the merge — re-run `check_cards.py` after consolidation as normal.
