# Unit A — editor pass (adversarial)

Independent editor; full checklist run per card, checks 18–24 per row.

## Verdicts

| Card | Verdict | Notes |
|---|---|---|
| A1 (4-belt grouped reveal) | **PASS** | 4 uncued answers under one c1 = exactly at the rule-23 cap; rows `<br><br>`-separated; rows lead with their cloze and hide everything (not rule-22's frame shape); **no printed ordinals**, so no position-cued row for `step_recitation` to bite. Count ("four locations") named in stem per §6. List complete vs source: highlight enumerates exactly 4 placements. Head-to-foot order is visible scaffolding + `Cue:`, never a clozed ordinal (rule 26). |
| A2 (four-point harness) | **PASS** | Crisp named-technique cloze, "fashion" labels the slot, no hint needed. Verbatim in highlight. |

Cards in: 2. Cards out: 2. Rewrites: 0. Drops: 0.

## Judgement calls (the assigned adjudications)

1. **A2's stem reveals A1's row 1 — adjudicated, kept, but the drafter's mitigation claim is WRONG.**
   The reveal is real: in the shuffled megadeck A2 ("belts over the patient's shoulders and chest…") can precede A1 and seeds one of A1's four answers. It does not sink A1: the card remains a genuine 4-item production with one row softened, and A1 is passable cold with or without A2 (the leak eases, it does not self-answer). Every restructure is worse: merging the harness fact into A1 as a c2 would print A1's ENTIRE answer set visible on the c2 card (a full-list reveal every review — the exact defect editor check 26 hunts); renaming A2's subject by position ("the FIRST belt…") makes a position-cued blank (rule 26 territory) and still demands order recall. A2's fact is *about* that belt; its stem must name it.
   **Correction to the notes file's F3 flag:** "bury-siblings mitigates" is false — A1 and A2 are separate NOTES, and bury-siblings only buries cards of the same note. The honest mitigations are (a) the leak is 1-of-4, and (b) interleaving makes the adjacency infrequent. Recorded here so the consolidation pass doesn't repeat the claim.

2. **The not-limit-breathing caution is CORRECTLY a Pitfall, not a cloze.** Both cloze directions decode via everyday knowledge: hide "limit breathing" and the visible "chest belts + too tight" hands it over (non-knower decodes); flip it (hide *which belts* carry the caution) and visible "breathing" decodes "chest" just as fast. The two halves mutually decode outside the book. A forced-choice hint would need an invented distractor the source never offers (no honest second option on p745). A self-answering row is padding (Parker 2026-07-30 corollary), so tested-by-teaching on A2's back is the strongest honest home. Upheld.

3. **Grouped reveal mechanics verified:** ≤4 uncued (exactly 4), `<br><br>` between all rows, no ordinals printed or clozed, count in stem, order derivable head-to-foot (taught in `Cue:` — a legitimate rule-23 handle, not a leak, since it regenerates order rather than membership).

4. **A2 `numeric: true` on "four-point harness"** — a name, not a count; kept as the drafter's conservative flag (costs one human glance, verified verbatim p745). Not worth churning.

## Hand-off flags

- **F1 (unchanged from drafter):** fluid-resistant-mattress infection-control fact is unmarked context — one sentence to Parker at hand-off if he wants it; no card.
- **Grounding audit clean:** A1 Why (crash protection) and both stems verbatim-faithful to mark 0's highlight/context. Both cards `numeric: true`, verified `p745`.
- No cross-unit Distinguish lines on this unit; nothing for the consolidation pass.
