# Unit E — editor pass (adversarial)

Independent editor; full checklist run per card, checks 18–24 per row.

## Verdicts

| Card | Verdict | Notes |
|---|---|---|
| E1 (sheet-not-clothing negation) | **PASS** | Rule-21(b) shape exactly: visible rejected alternative anchors the blank. Not open-set — see call 1. `::your grip` is a clean slot-label (names the form, never restates). One-way upheld (reverse is open-set). |
| E2 (transfer setup values) | **REWRITE** | c2 ("firmly against the bed's side") demoted to visible scaffold — non-knower decodes it from physics alone; c1 blank tightened 10 → 7 hidden words. Now a single-cloze card. |
| E3 (kneel + increments) | **PASS** | Both rows cold-solve; `::pacing` adjudicated a slot-label, not a leak — see call 3. |

Cards in: 3 notes (5 cloze cards). Cards out: 3 notes (4 cloze cards). Rewrites: 1. Drops: 0 (the demoted clause stays on the card's front; nothing left the unit).

## Judgement calls (the assigned adjudications)

1. **E1 is NOT open-set (rule 16).** Cold universe for "pull on ___" in a bed: the patient's clothing (visibly rejected), the patient's body/limbs (the floor-drag habit), or the bedding. The negation pattern "pull on ___ — never clothing" plus the bed setting plus the `your grip` slot-label collapses it to the bedding, and the book's own contrast ("Rather than dragging the patient by his or her clothing, use the sheet or blanket…") is the exact pair the card shows. A knower writes "the sheet or blanket under them" first try; a non-knower's best decode ("the sheet?") is partial and unforced. Essentially-one producible answer = passes. Considered and rejected making "clothing" a c2 sibling: the reverse blank is half-decodable (clothes-drag is the canonical rejected grip once "sheet or blanket" is visible) and doubles volume on one binary fact; §10 cards the carve-out one-way.

2. **E2's c2 was a freebie — demoted, not dropped.** "Hold it ___ against the bed" / "___ (how and where)" is answered by common sense: transferring a patient across surfaces obviously requires the surfaces adjacent and the wheeled one held still. Fails check 15's non-knower test; Parker's corollary applies ("a row that answers itself is padding, not coverage — drop it"). The drafter pre-authorized this exact downgrade (their flag F2). The highlighted clause now rides VISIBLE in E2's stem ("…and hold it firmly against the bed's side") — stronger than Back-Extra burial: still read every review, and it feeds situational context to the height blank without leaking it. The height value keeps knower-discrimination through its "or slightly higher" tail.

3. **E3's `::pacing` is a slot-label, not a leak.** It cannot replace the answer ("drag the patient pacing" is nonsense) and it names only the answer's FORM — a manner-of-progress word. It is also necessary: without it, "drag the patient ___" is open-set (smoothly / slowly / carefully / in increments all fit; rule 16). With it the space narrows to tempo-answers and only the knower lands "in increments" — and the same page's OTHER manner, "smoothly and evenly," belongs to the rolled-bedding glide, which the stem excludes by naming the stretcher-to-bed drag. Hard-but-forced. Kept.

4. **The digits check (assigned):** E1's Cue "6 in (15 cm)" is verbatim from the p766 context ("6 inches (15 cm)"), `numeric: true`, `verified_against: "p766"`, `verified_by: "agent"` — all present and correct. The context's OTHER figure, "20 in (50 cm) of the bed's edge," is used on no card (drafter's choice, upheld: it belongs to a clipped, unmarked setup sentence). E3's conservative `numeric: true` for "a third person" verified p766.

5. **E2 self-check after rewrite:** cold-solve — "place the stretcher ___ (height) relative to the bed" forces a height relation; the knower's full value ("at the same height or slightly higher") beats the non-knower's default guess ("same height"), which misses the licensed higher-than allowance. Blank now 7 words (under the ≥9 warn line).

## Hand-off flags (for the consolidation pass — flagged, NOT fixed)

- **CONFUSABLE NEAR-TWIN ACROSS UNITS (the one real cross-unit find):** E2 teaches stretcher→bed at "same height **or slightly higher**"; unit M's card M2 `Cue:` carries the draw-sheet (bed→stretcher) prep at "same height **or slightly lower**." Opposite tails on the same axis = prime interference (non-negotiable #7). Neither unit's marks ground the other's value, so neither drafter could legally write the cross-link. Consolidation should add mirrored `Distinguish:` lines from M's grounding (e.g., on E2: "bed→stretcher draw-sheet prep runs same height or slightly LOWER — direction decides the tilt").
- **No duplicate found** between E2's `Cue:` ("the transfer itself is a body drag") and unit J's "long-axis body drag" (different fact: vehicle-removal drag type) or unit M's transfer cards (opposite direction, bed→stretcher). Checked by grep across all unit card files.
- **F1 (unchanged from drafter):** the lateral-leverage / side-by-side pull passage is unmarked and clipped; not drafted. If no other unit's marks cover it, it is hand-off-mention material only.
