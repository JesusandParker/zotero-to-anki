# K_rapid_extrication — adversarial editor pass

Independent fact pass re-run from mark 18's context + VERIFIED BLOCKS 2-3. All ten
context propositions are covered (tested or deliberately supporting); the six table
items were re-counted in the context (6 exactly, matching VB3). The real findings were
two leaks and one ambiguity: an invented sub-group name that lexically handed over two
of its own members, a Back-Extra line that spelled a sibling's literal answer, and a
vignette that a knower of unit J could legitimately answer with the wrong technique.
7 cards ship. No drops.

## Verdict table

| Card | Verdict | Reason |
|---|---|---|
| K1 environment sub-group | REWRITE | Invented name "scene hazards" self-leaked members (rule 30's group-name check): "scene" → "the vehicle or **scene** is unsafe," "hazards" → "**hazardous** materials are on the scene." Renamed to "the three that arise from the environment, not from the patient" — no shared roots with any hidden member; categorical cueing (what a semantic-partition name is FOR) retained. Name stays visible, never clozed. Roster + bolding verified. |
| K2 patient-factors sub-group | REWRITE | Same rename in its cross-reference ("Beyond the three from the environment"). "Patient factors" itself is clean — its rows keep "the patient" as VISIBLE shared scaffold (rule 18's good shape), so the name overlaps no hidden text. Rows hide more than they show and the set is counted — cold-solvable as a produce-the-set card. |
| K3 timing trade-off | PASS | 1-minute half verbatim in m18 context; 6-to-8 half carries VB2's exact visual_source; each number anchors the other's card (no husk); unit "minutes" / parallel visible number satisfy rule 27 without hints. Digits re-verified. |
| K4 spine-risk negation | REWRITE | Distinguish line handed K5 its literal answer for K5's literal scenario ("a patient able to stand and pivot… it is safer") — rule 13. Dropped the line; Why retained. The two-blanks-one-number grouping is correct and kept: a c1/c2 split would let each half decode the other ("do not use" ⇒ greater risk, and vice versa); both blanks carry forced-choice binary hints, so neither is starved. |
| K5 stand-and-pivot polarity | REWRITE | Back-Extra Why opened with a dangling "it" (Layer-A #2); replaced with the noun ("standing and pivoting keeps…"). Card itself sound: binary with mandatory forced-choice hint, comparator ("than performing a rapid extrication") grounded by the passage flow. |
| K6 team of three | PASS | Rule 27 hint in Parker's own format (::number of providers); "three… knowledgeable and practiced" verbatim p803. Why line stays within the passage's own claims. |
| K7 gasoline vignette | REWRITE | One-answer gap: the stem never ruled out unit J's one-person vehicle removal, which is licensed for exactly this danger class when alone — a knower of both units could hesitate or answer with the drag. Added "your crew is at the vehicle with you" (rules 2/§12a: every discriminating clue in the stem), deliberately UNNUMBERED so K6's "three" is never spelled on a sibling's visible side. Exemplar freshness verified: gasoline appears on no sibling (rosters name "hazardous materials"/"fire," never fuel), so the mapping step — gasoline ⇒ hazmat + fire danger ⇒ technique — is genuinely his to make. |

Counts: 2 PASS · 5 REWRITE · 0 DROP → 7 cards.

## Load-bearing judgement calls

1. **The rename is lexical, not semantic.** Any accurate name for {scene unsafe, hazmat,
   fire} overlaps them in MEANING — that is what a chunk name is (the radio report's
   "patient picture" cues its members categorically too, and rule 23.1 blesses it). What
   rule 30 forbids is the name containing a member's own words, which "scene hazards"
   did twice. "Environment" shares no root with any member.
2. **K1-stem vs K7 (the hunt item):** K1's visible stem pairs "rapid extrication" with
   "six situations," so a K1-studier can reach K7's answer — by the knowledge chain the
   card is built to exercise (instance → category → technique), not by exemplar
   recognition. That is teach-then-test, not a rule-13 leak; the leak would be gasoline
   appearing on a sibling, and it does not.
3. **K4/K5 mechanism coherence accepted.** K5's Why implies K4's direction (manual
   support → spine-movement risk). These are consequences of one causal cluster; only
   the verbatim scenario+answer giveaway (K4's old Distinguish) was removed. Stripping
   all mechanism from the backs would gut them (check 11) for a marginal gain.
4. **No anchor note exists, correctly** — TABLE 8-3 is an unordered set with invented
   sub-group names, so rule 23.1 forbids one. The two sub-group notes plus rosters are
   the whole membership family. Do not add one.
5. **P6 (device delay = contraindication) stays card-less: ENDORSED** (drafter's rule-12
   call). It is the rationale of the table, tested behaviorally by K7's decision and
   taught on K1/K3/K7 backs; a standalone polarity card would re-test the same claim.

## Pre-cleared detector warnings (for the judge)

- `husk_groups` on K4: two spans under c1, each with its own forced-choice hint and
  independent visible cue ("risk of spine movement than device immobilization" / "when
  no urgency exists") — grouped BECAUSE splitting would leak, per above.
- `row_label_tautology` on K1/K2: post-rename the group labels cue category only and
  share no words with hidden rows ("patient" in K2 sits in the visible scaffold).
- `overloaded_group`: 3 uncued per note, under cap.

## Drafter flags adjudicated

- **Invented sub-group names: ENDORSED as scaffolding, with group A renamed** (self-leak
  fix above). Both names visible-only, never clozed; no anchor note.
- **P13 urgent-move triggers (altered LOC / inadequate ventilation / shock / extreme
  weather) UNMARKED: ENDORSED.** No card leans on it (K7 re-verified: its answer rides
  the marked table mapping, visual_source correctly null). Stays Parker's one-sentence
  call at hand-off.
- **6-to-8-minute digits via VB2: ENDORSED** — re-checked against the licensed quote;
  visual_source string matches the license verbatim.
- **Count verification p803-804: ENDORSED** — six items re-counted in m18's context.
