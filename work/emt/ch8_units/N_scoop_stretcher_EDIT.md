# Unit N — Editor pass (adversarial), 2026-08-12

Scope: mark 21 (p832, Skill Drill 8-11 caption; `page_sparse` — context carries captions,
numbered list, and the unmarked "Other Carries" prose). 4 notes, **5 Anki cards** (was 4:
#3 became two-way).

## Verdict table

| Card | Verdict | What was wrong / adjudicated |
|---|---|---|
| #0 size-while-separated | REWRITE (hint) | "separated" is a two-pole state blank (separated vs locked) with no forced choice — rule 4 / check 13. Now `{{c1::separated::separated or locked}}`. The visible "halves… still" already leaned toward the answer, so the hint costs nothing and makes the answer space explicit and fair. |
| #1 one-side-at-a-time + pull points | PASS | The task-named leak hunt ("scoop" visible while asking one-side-at-a-time): adjudicated NO decode — the card never says "halves," and inferring one-side-at-a-time from knowing a scoop splits apart is domain knowledge, not card leakage. Anti-leak paraphrases verified in place ("lifting the patient slightly" for the source's "the patient's *side*"; partner's "slides that half" kept off the front). Clozes are landmarks/manner (rule 26 residue), not motion. |
| #2 lock discipline + pinch | PASS | "pinching" is partially decodable from "— and your own fingers" (what else at a latch?), accepted deliberately: it is grouped under c1 with the genuine retrieval ("one at a time"), it is the plate's own caution carded for exposure, and stripping "your own fingers" would delete content to protect a blank. "one at a time" is produced content, not a polarity — no forced-choice owed. |
| #3 strap-before-transfer vignette | REWRITE (two-way) | **The MUST-TEST fact per the drafter's own fact pass is "straps BEFORE transferring" — and "Before" sat visible as scenery, never retrieved** (check 4; encoding specificity: the untested direction doesn't train). Now `{{c2::Before::before or after}} transferring…, {{c1::apply and tighten the straps::securing step}}` — two cards: the action vignette (unchanged face) and the timing binary with forced choice. The timing is exactly the discriminator the `Distinguish:` teaches (scoop straps before vs direct carry straps after), so it earns retrieval, not just teaching. |

## Fact-pass re-run (mark 21)

All MUST-TEST facts clozed; the timing gap above was the one miss. "Other Carries" prose
correctly used as Back-Extra support only.

## Judgement calls

1. **No-indication constraint VERIFIED (the drafter's own flag):** no card fronts or
   backs assert when to *choose* a scoop. #0's `Ex:` states only that the scoop is one
   of four ground-to-stretcher options — verbatim from mark 21's context, a roster of
   alternatives, not an indication. Nothing claims spinal use or patient selection.
2. **The four-method count in #0's `Ex:` — defensible, recorded.** Context reads "Place
   both the backboard and the patient onto the stretcher" as the *completion* of the
   backboard route (it cannot stand alone as a method), giving four: direct body carry /
   log roll or drag onto backboard / scoop / blanket. If the printed page bullets it as
   five, the fix is the Ex line, not any cloze. Flag stays for the figure/consolidation
   stage since the original bullet layout is invisible in extraction.
3. **"one side at a time" / "one at a time" as manner phrases, `numeric: false`** —
   drafter's call CONCURRED. Neither is a bare count in an attributive slot (rule 27's
   scope); both are adverbial manner. If the gate's value detector fires, the words are
   verbatim in mark 21's context (p832).
4. **"wheeled stretcher" on #3** — a clarifying qualifier, not an invented fact: the
   destination "stretcher" in the drill is the wheeled ambulance stretcher, and leaving
   it bare would collide with the scoop (itself a stretcher) on the same card.
5. **Sibling near-parallel answers** (#1 "one side at a time", #2 "one at a time") are
   the source's own parallelism — different facts (positioning vs locking), each cued by
   its own condition; not a rule-13 defect.

## Hand-off flags

1. **The indication gap is real and stays open:** mark 21's context never says WHEN to
   reach for a scoop. Per R40 the permitted move is the one-sentence ask at hand-off —
   suggested wording: "the scoop drill pages you marked never say when to choose a
   scoop over the alternatives; want the indication carded from its page elsewhere in
   the chapter?"
2. After my rewrite, #3 makes TWO cards (action + timing). The direct-carry strap
   timing still lives ONLY here (unit M carries none by design) — consolidation should
   keep exactly this one home.
3. Figure stage: Skill Drill 8-11 composite (4 panels) to all backs; #2 already matches
   the plate's own exclamation caution.
