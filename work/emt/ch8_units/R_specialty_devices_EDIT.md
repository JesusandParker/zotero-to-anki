# R_specialty_devices — adversarial edit (marks 26–28)

Fact re-pass: M1 flexible-forms-RIGID → **was untested (cue-only on every face); now c2** ·
M2/M3 conforms/no-farther → R1 c2 ✓ · M4 backboard-alternative → **was untested (cue-only);
now c3** · M5 air-removed → deliberately visible scaffold (agree: with "vacuum" in the
term, clozing it is a rule-20 self-answering row; it is the cue) · M6 mold → R2 c2 ✓ ·
M7 Stokes binding → R3 c1/c2 ✓ · M8 indication → tested in the §9 direction (situation
visible → device produced) on both R3 cards ✓. Nothing beyond the three marks' contexts.

## Verdicts

| Card | Verdict | What broke / what held |
|---|---|---|
| R1 flexible stretcher | **REWRITE** | Check 4: the signature paradox (flexible → forms a RIGID stretcher) was tagged MUST-TEST in the drafter's own notes yet "rigid" sat visible on both faces — never produced. Fix: `forms a {{c2::rigid}} stretcher` grouped with the property under c2 (2 spans, both cued by the visible term; not R10 — the anchor term stays visible). Grouping beats a separate c3: hidden together, the conform-property cannot describe "rigid" back into existence. Also tightened the c2 property to `conforms around the patient's sides and no farther` (9 → 8 tokens, faithful to M2+M3, crisper to reproduce; the "them" referent problem that forbade splitting M2/M3 disappears with it). |
| R2 vacuum mattress | **REWRITE** | Check 4: M4 (alternative to the BACKBOARD — the marked sentence's clipped head, and the device's clinical role) rode visible on every face; the drafter's note claims it MUST-TEST but only used it as cue. Fix: `{{c3::backboard}}` — device→role direction now produced; knower-forced (the book states the pairing twice), non-knower unconstrained. Distinguish's "long spine board" mention is back-side reinforcement, not a leak. |
| R3 basket stretcher / Stokes | **PASS** | Both rows cold-solve; `::eponym` is a model rule-18 form label. |

## Judgement calls

- **R2 c1 derivability (vacuum ↔ "air is removed"), adjudicated ACCEPT:** the device is
  descriptively named, so any honest two-way carries partial decode of "vacuum" — but the
  knower-specific recall survives ("mattress", whole-body backboard alternative, vs the
  vacuum SPLINT and the other backboard alternatives). Rule 3's precision clause protects
  two-way definitions from over-correction; the reverse fix (clozing "air is removed")
  is the rule-20 padding the drafter correctly refused.
- **R3 indication stem, adjudicated HONEST CUE, not crutch:** "uneven terrain / remote /
  vehicle-inaccessible" is situational (where and when), not definitional — nothing in it
  describes a basket or decodes "rigid basket stretcher"; "Stokes" is opaque. This is
  rule 3's median working as designed, and the §9 shape (cue visible, payoff clozed) is
  the profile's own indication pattern. The reverse direction (device → recite indication)
  correctly not carded (not a real goal; semi-derivable).
- **Cross-device `Distinguish:` lines, grounding audited:** R1's (confined-space ← mark 26
  context; basket/uneven-terrain ← mark 28) and R3's (mirror) each draw on BOTH marks →
  `from_idx: [26, 28]` on R1/R3 **correct**; R2's comfort/padding-equivalence claim sits
  wholly inside mark 27's context → `from_idx: [27]` **correct**. All back-side cross-links
  are the blessed §8 pattern (post-answer), not check-16 sibling-leaks — no scenario
  exemplar is reused on any front.
- R1 c2 antonym-guessability ("flexible … forms a ___" invites "rigid") accepted: the
  contrast IS the fact, hard-but-forced for a knower, and the alternative (leaving it
  visible) tests nothing.

## Hand-off flags

- C7 (spinal suspicion → backboard FIRST, then into the basket) is unmarked context, taught
  as R3 `Pitfall:` only — decision-shaped and card-tempting; needs Parker's word to test
  (rule 29).
- No numbers anywhere (`numeric: false` correct on all three).
