# P_geriatric — adversarial edit (mark 23)

Fact re-pass from P_geriatric.json: M1 dislocation → P1 c1 ✓, M2 fracture → P1 c1 ✓,
M3 thinner → P2 c1 ✓, M4 tears → P2 c2 ✓, M5 bruising → P2 c2 ✓, M6 grip caution →
P2's situational frame (rule-12 meaning-duplicate of M3–M5; agree with drafter).
Nothing on either card exceeds mark 23's context. All checks incl. 18–24 run per row.

## Verdicts

| Card | Verdict | What broke / what held |
|---|---|---|
| P1 dislocation/fracture | **REWRITE** | Hints `::joint injury` / `::bone injury` adjudicated as leaks, not slot-labels (checks 9/15, rules 4/18). "Bone injury" has essentially ONE member in EMT vocabulary — a fracture IS a broken bone, so the hint survives the replace test in one direction only; and the individually-keyed pair hands over the partition, so knowing either answer yields the other by elimination. Unhinted is worse (open-set drift to "pain"/"skin tear" + direct interference with P2's near-identical frame). Fix: one shared form-label `::musculoskeletal injury` on both blanks — grounded in the context's own musculoskeletal-vs-integumentary framing, ≥4-member category (sprain/strain/dislocation/fracture), kills the drift and the P2 crossover without decoding either answer. Also added the reciprocal `Distinguish:` (pulling→joint/bone vs gripping→skin) so BOTH siblings pin the pair, per recipes §8; P1 previously left interference control to P2's back alone. |
| P2 thin skin → tears/bruising | **PASS** | c1 (binary, `::thicker/thinner` forced-choice per rule 4) and c2 ("tears" + "bruising", 2 crisp one-word answers, one number, load 2 ≤ 4) both cold-solve per row. |

## Judgement calls

- **P2 c1 mild derivability, adjudicated and accepted:** on the c1 card the visible
  consequences ("tears and bruising") semi-derive "thinner." Inherent to the honest two-way
  shape (rule 3 precision protects two-way definitions from over-correction); hiding
  property + consequences under one number would bundle mutually-supporting spans toward an
  R10 husk. Same class as R_specialty_devices' vacuum↔air call.
- **Cross-interference P1↔P2:** held three ways after the rewrite — each stem is cued by its
  own verb (pulling vs gripping), P1's hint now excludes integumentary answers, and both
  Back Extras carry the mirrored `Distinguish:`. Answers appearing on the sibling's BACK is
  the blessed §8 cross-link, not a check-16 sibling-leak (no scenario exemplar is reused).
- P1 keeps "may cause" (the book's hedge for "in some older patients").

## Hand-off flags

- None blocking. No numbers (`numeric: false` correct on both).
- C4 (cannot lie flat / extra padding) remains context-only, taught as P2 `Pitfall:` — if
  Parker wants it TESTED, that needs his word (unmarked; rule 29).
