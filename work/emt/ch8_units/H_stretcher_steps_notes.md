# Unit H — loaded stretcher on steps: undercarriage rule (mark 12)

1 note → **2 Anki cards**. Gate not run by this drafter (orchestrator runs it after the
editor pass); cloze answers hand-checked word-by-word against mark 12's highlight.

## Fact pass

| mark | proposition | verdict | where it landed |
|---|---|---|---|
| 12 | loaded stretcher carried DOWN A SHORT FLIGHT OF STEPS → FIRST retract or raise the undercarriage | **MUST-TEST** | c1 (the action; "first" kept visible) |
| 12 | NOT necessary when lifting over a curb, a single step, or an obstacle of similar height | **MUST-TEST** (the exception; closed 3-item set) | c2 — three spans under one number, inline, so the blank count is visible |
| 12 context | FIGURE 8-13 caption repeats the exemption verbatim | SKIP — duplicate of the exception (rule 12, dedupe by meaning) | — |
| 12 context | at the ambulance: roll the front wheels onto the floor, advance the stretcher until the SAFETY HOOK catches it; foot-end EMT lifts and… (cut off) | SKIP — unmarked, different topic (loading), sentence truncated | flagged below (tempting) |

## Archetype

**Negation/exception (§10), exactly as the brief asked.** One note, two directions:
- c1 = the required preparation ("retract or raise the undercarriage"), hint
  `::stretcher adjustment` — a category label chosen deliberately to kill two risks:
  the open-action-blank shape (rule 16: "before steps, first ___" admits many EMT
  actions) and cross-unit interference with unit G ("position the strongest at the head
  end" is a plausible wrong fill for an unhinted blank in a steps stem; the hint steers
  to equipment, leaking nothing).
- c2 = the carve-out set, clozed per the recipe with the negator loud
  (`is <b>not</b> necessary` — `<b>` because `<u>` is disallowed HTML). Three separate
  c2 spans inline ("over ___, ___, or ___") so Parker sees he owes three answers without
  a count hint.
- The contrast (short flight of steps vs curb-height obstacle) stays fully visible in
  the stem, per the brief.
- Sentence 2 opens "The same preparation…" — refers to the c1 blank without naming the
  undercarriage, so the exception sentence cannot leak c1's answer on the c1 card.

## Interference engineering

The shuffled deck now has two "steps/stairs → do what?" families: unit G (backboard
carry, provider positioning) and this rule (wheeled stretcher, undercarriage). Anchors:
this stem names the **loaded stretcher** and a **short flight of steps**; the
`Distinguish:` line separates the families explicitly (full stairway → patient secured
to a backboard, carried down to the prepared stretcher; short flight → the wheeled
stretcher itself, undercarriage up).

## Grounding note for the editor

`from_idx` is **[10, 12]**: every cloze answer grounds in mark 12; mark 10 (also this
drafter's assigned mark, unit G) is cited because its context ("secure the patient onto
a … backboard … Carry the patient on the backboard down the stairs to the prepared
stretcher") is what grounds the Back-Extra `Distinguish:` line's backboard-stairway
half. Nothing on the card comes from outside marks 10–14.

## Flags for hand-off

1. **Unmarked-but-tempting (context of mark 12):** the ambulance-loading detail — advance
   the stretcher until the **safety hook** catches it. Testable nugget, unmarked, and the
   context sentence is cut off mid-thought; want anything from it?
2. **Numeric verification:** the card's quantity content ("a single step"; the 3-member
   exemption set) checked verbatim against mark 12's highlight, p789 →
   `numeric: true`, `verified_against: "p789"`, `verified_by: "agent"`;
   `needs_human_check` left `false` for the report to derive.
3. The book states no mechanism for WHY the undercarriage must come up for steps — no
   `Why:` line was invented (Layer A #7). Back Extra is a single `Distinguish:` line.
