# Q_bariatrics — adversarial edit (marks 24–25)

Fact re-pass: M1 definition → Q1 c1/c2 ✓; M2 baros = weight and M3 iatreia = medical
treatment → `Parts:` line, **verified verbatim against mark 25** ("the Greek words baros,
weight, and iatreia, medical treatment") — both glosses correct. Rule 0 synthesis of the
two consecutive marks into one note is right; `from_idx: [24, 25]` correct. Nothing beyond
the marks' context (C2's $147B/$1,429 correctly skipped — unmarked numbers, rule 29).

## Verdicts

| Card | Verdict | What broke / what held |
|---|---|---|
| Q1 bariatrics two-way | **REWRITE** | c2 blank "the management (prevention or control) of obesity" is a 7-token span with a NESTED PARENTHETICAL — below the mechanical warn line but exactly the shape nobody reproduces verbatim (check 6 / R12 spirit; §4 target is 3–6 words). The drafter was right that the parenthetical cannot sit visible (it paraphrase-leaks "management", rule 3) — but it doesn't have to be in the blank either. Fix: c2 → `the management of obesity` (4 words, discriminator intact — "obesity" stays inside the blank, since with it visible the retrieval would collapse to the filler word "management", rule 22); the parenthetical moved to a new `Meaning:` line ("management here spans both prevention and control"). Also normalized `Parts:` to parallel glosses: `Greek baros (weight) + iatreia (medical treatment) — the medicine of weight.` |

## Judgement calls

- **`Meaning:` line vs "never re-define the term":** it glosses one word INSIDE the answer
  (the book's own parenthetical), not the term bariatrics — this is check 6's prescribed
  home for fuller wording, not a Layer-A #5 re-definition. `Meaning:` is in the blessed
  label set.
- **`Parts:` on a yellow card:** SHARED_BRIEF's label list omits `Parts:`, but
  note-format.md (the canonical vocabulary the brief points to) licenses it "on any card
  whose term genuinely decomposes." Kept — it is the designed home for mark 25, and the
  parent's brief asked only that its content be verified (it is, verbatim).
- **Etymology not clozed:** parent-adjudicated shape (mark 25 covered via `Parts:`, not
  tested). Agree — clozing Greek roots would be trivia; the mark is fully covered on the
  note it grounds.
- **c1 hint `::medical specialty` kept** despite partial redundancy with the visible
  "branch of medicine": it constrains the answer's REGISTER (the one-word specialty name,
  not a description), was explicitly specced to the drafter, and leaks nothing. Logged
  because §4's "skip if the stem already forces" arguably applies; harmless either way.
- "and allied diseases" stays visible as the c2 card's non-leaking anchor tail —
  deliberately not clozed (a `c3` on it would be an open-set micro-fact; as scenery it
  cues without leaking).

## Hand-off flags

- C5 (EMT back-injury toll of bariatric patients) remains the one card-tempting unmarked
  context fact — surfaced for Parker per rule 29, not drafted.
- No numbers on the card (`numeric: false` correct).
