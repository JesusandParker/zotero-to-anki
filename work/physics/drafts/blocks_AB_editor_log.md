# Blocks A+B — adversarial editor log (Giancoli 7e ch. 1, segment 1)

Input: `block_A.json` (7 notes, idx 0–8) + `block_B.json` (10 notes, idx 9–19).
Output: `blocks_AB_edited.json` — **18 notes** (11 PASS, 6 REWRITE, 1 ADDED by restructure, 0 DROP).
Every card run through the full checklist; checks 18–24 run per row. Drafter notes read only AFTER forming my own verdicts.

## Per-card verdicts (index = position in blocks_AB_edited.json)

| # | Card | Verdict | What changed and why |
|---|---|---|---|
| 0 | classical vs modern physics (idx 0) | **PASS** | Classify direction is the licensed shape: producing 7+7 uncued topics would break check 25/rule 23 (no mnemonic handle), so topics stay visible as the cue and only the two division names are produced (rule 22's "not a violation" clause). Ran check 22 per row: neither topic list contains its own category word — not self-answering. Forced-choice `::classical/modern` hints are the rule-4 licensed form for a binary (check 13); `visible_stem` blanks without the hint, so no mechanical leak either. 2 answers under c1 = one retrieval (check 25). Rows `<br><br>` (check 21). Known residue (drafter agrees): a two-option classify always yields the second row by elimination — inherent to the archetype, accepted. |
| 1 | Aristotle/Galileo lifespans (idx 1,2,3) | **REWRITE** | (a) **Set `image_side: "front"`** — Parker's idx 3 comment is the spec ("I wanna see you quizzing me on when Aristotle is born in this beautiful piece of artwork at the bottom"): artwork visible WHILE quizzed = front; The School of Athens contains no dates, so front placement leaks nothing (note-format's own license). `image` left null for the consolidator to attach. (b) Cue "B.C. antiquity to the Renaissance" → "to the 1600s" — "Renaissance" describes the *painting* (c. 1510) in the cited context, not Galileo's working era; "the 1600s" is the context's own phrase (check 3). (c) Added `Ex:` naming the plate (Raphael, c. 1510, Aristotle center in blue beside Plato) — grounded in idx 3's caption context; makes the front artwork self-explaining (congruence preference) and carries idx 3's one non-image fact. Dates verified verbatim vs highlights: 384–322 B.C. / 1564–1642 ✓ (checks 3, 18). Margin comment honored as scope: descriptors ("argued rest is natural", "first true experimentalist") deliberately left unclozed — check 4 cleared per the comment "I'm just trying to know and memorize when Aristotle lived and Galileo lived". c1/c2 split = each card anchors the other (check 19 ✓). |
| 2 | theory — lexicon (idx 4, +8) | **REWRITE** | Grounding repair (check 3): "broad" and the law-contrast Distinguish were supported only by page-26 text the card didn't cite; drafter had parked the trail in notes, which is not citation. Fix: cite `from_idx [4, 8]` — idx 8 is *lexicon* kind, so R36 kind-agreement still holds (verified: lexicon_check passes), and its context supplies "more general picture" + the law sentence. The **model clause was cut** from Distinguish (its grounding lives in idx 6/7, which this card cannot cite without tripping R36); theory↔model contrast remains taught on card 4's Distinguish. Answer tightened to "a broad, testable explanation that **organizes** observations" (plainer than "orders" on a stranger-read; same sense as "explain and order the observations"; 7 words — plain·crisp·faithful, check 30). Ex swapped from a definition-quote (which restated the answer) to a true usage example (atomic theory, grounded idx 4). Why line now carries the anchor quote's exact claim ("never derived directly from observations") — **answer agrees with the in_source anchor** (`theor`, p24) ✓. |
| 3 | da Vinci's notebooks (idx 5, 2) | **PASS** | c1 (whose notebooks) / c2 (first references to what) both cold-solve; "architecture and building" is situational anchor, not a definition of the hidden span (check 15 median: knower-can / non-knower-can't-produce-exactly). Drafter rightly kept "the great Renaissance artist…" OUT of the stem (would let a non-knower decode c1). I deliberated cutting "(1564–1642)" from Distinguish as a sibling-answer restatement of card 1's c2 — kept it: it is grounded, cited (idx 2), concretizes "a century before," and check 16's mechanism (pattern-matching a neighbor at test time) doesn't apply to post-answer backs; house style embraces part-and-whole re-exposure. Galvani Ex = grounded context enrichment, correctly NOT carded (unmarked, rule 29). |
| 4 | model — two-way def (idx 6, 7) | **PASS** | c2 "an analogy to something already familiar" = 6 words, crisp ✓ faithful to "a kind of analogy or mental image… something else we are already familiar with." Both directions cold-solve. Distinguish (theory contrast) grounded in cited idx 7 ✓. Why adds purpose (not a re-definition — check 11 ✓). Two-way def is explicitly never "fixed" as a leak (rule 3 precision). |
| 5 | law — two-way + form (idx 7) | **PASS** | c1/c2/c3 are *different facts* of one definition, not a list fanned across numbers (rule 24 "not a violation"). Each of the three fronts cold-solves with the others as anchor ("concise but **general**" forces law over principle on c1). All three spans verbatim in the cited context, including F = ma (clipped from the highlight — the reason idx 7 is PARTIAL — but present in context). Pitfall descriptive-vs-prescriptive grounded in idx 7's context. PARTIAL grounding → `needs_human_check` derivation is verify_report.py's job (not asserted here, per contract). |
| 6 | principle — lexicon (idx 8) | **PASS** | Check 30: plain ✓ crisp (8 words, at the cap) ✓ faithful — keeps the discriminator (less general than a law) and matches the Ex sense (Archimedes' principle). Anchor `in_source` p26 resolves in lexicon_evidence.json (R37 ✓) and the answer agrees with the quoted sentence ✓. Ex present, quote-with-term is the §4b house Ex style. |
| 7 | interpolate — lexicon (idx 9) | **REWRITE** | Cue precision (check 3): the book says the *difficulty* of interpolating is why the reading is only good to ~the smallest division — Cue now says "the difficulty of interpolating … is why…" instead of crediting interpolation itself. **Clearance:** Distinguish vs *extrapolate* is authored outside-knowledge — cleared under the lexicon lane's authored license (nearest-confusable line is exactly check 30's concern); the card is external-anchored so it reaches Parker's eyes via the Vocabulary block anyway. `external` is the honest anchor (no evidence entry exists; the book's gloss is a parenthetical the finder didn't extract, and in_source without an evidence entry would be R37-fabrication). Expected R35 hard error — see "Expected errors" below. |
| 8 | estimated uncertainty — lexicon (idx 10) | **PASS** | Answer "the stated ± it might be off by" — plain on a stranger-read, crisp, faithful ("it" resolves to "a measurement" in the same sentence — no dangling deixis). Ex quotes the encounter sentence ✓. Distinguish ↔ percent uncertainty is the endorsed confusable cross-link (mutual with card 9). Same expected R35 as #7. |
| 9 | percent uncertainty (idx 11 + 17, fold-in) | **PASS** | Cross-lane fold-in confirmed legal and right: purple idx 17 names the exact term yellow idx 11 defines; one two-way card covers both lanes; a separate lexicon card would be a rule-12 duplicate. lexicon_check's own message endorses the shape ("a fold-in must cite at least one yellow mark"). Both fronts cold-solve; c2 is formula-shaped and as crisp as the content allows. **Adjudication:** `numeric: false` upheld — the ×100 is structural to "percent," not a memorized value at wrong-digit risk; worked-example digits live on the back. idx 11 is PARTIAL → left **unverified on purpose** so verify_report derives the full human-check tier (I did not verify-stamp this one). |
| 10 | last-digit assumption (idx 12) | **PASS** | Two hinted slots under one c1 = one convention, 2 cued answers (check 25 ✓). Husk litmus (check 19) run: remainder "When a value's uncertainty is not stated explicitly, it is assumed to be ⟨amount⟩ in the ⟨position⟩ specified" is a real scenario stem, not scaffolding — each slot answerable with both hidden. Hints "amount"/"position" are pure slot-labels (check 9). Pitfall (8.80 trap) and Distinguish grounded in cited context; correctly taught-not-carded (unmarked, rule 29). |
| 11 | significant figures — two-way def (idx 13) | **PASS** | c2 "reliably known" = 2-word crisp discriminator. Both directions cold-solve; non-knower cannot decode "significant figures" from "reliably known digits." Ex/Distinguish grounded ✓, exemplars (23.21 / 0.062) fresh — not reused by the application cards (check 16 ✓). |
| 12 | "roughly 80 km" → one sig fig (idx 14) | **REWRITE** (Back Extra) | Text untouched — stem's "the number of significant figures is ___" is rule-27's licensed quantity-word form, and hiding the count inside that frame avoids the figure/figures plural leak (drafter had this right). **Changed:** old Distinguish taught the unhedged-80 case (now TESTED on #13), and per rule 25's no-restate clause a back must not hand over a sibling's tested count — replaced with a values-free Why (ties "roughly" to the reliably-known definition, which sits verbatim inside idx 14's own context) + kept the wording-decides Cue (names the axis, no values). Editor-verified: count and "place holder" are verbatim in the EXACT-grounded highlight → `verified_against` recorded (skim tier; numeric stays true). |
| 13 | unhedged 80 km → assumed ±1–2 km, two sig figs (idx 14) | **ADDED** (restructure, not a drop-fix) | **The one real coverage break found.** The second sentence of yellow idx 14 ("If there is no suggestion that the 80 is a rough approximation… within an accuracy of about 1 or 2 km… two significant figures") was drafted **taught-not-tested** (Back-Extra-only) — that is dropped coverage of part of a yellow mark (checks 4/8, rule 7; the drafter flagged it for the consolidator rather than fixing). The interpolation defense doesn't hold: rule 25 targets arbitrary lookup columns, and these counts are rule-derived — derivation IS the tested skill (the drafter's own defense of card 14's "hard-but-forced" answers). Shape: **separate note** (rule 24 — no cross-reveal with #12/#14), both spans one c1 group (2 answers, one convention), counts announced by the stem's "the number of significant figures is"; "1 or 2 km" carries its unit (rule 27 exempt). Backs of #12/#13/#14 audited: **no note's back reveals a sibling's tested count or notation** (the drafter's own good discipline, now held with all three cases tested). "As we will in this book" dropped (source artifact). Verified verbatim against the EXACT highlight → verified_against recorded. |
| 14 | 80.0 km → three sig figs (idx 15, +13, 12) | **REWRITE** (citations + stamp) | Text untouched: c1 (count, slot-labeled) and c2 (notation) are both derivable-only-via-the-convention — hard-but-forced, the intended skill; "80 km" visible vs "80.0 km" hidden is not a tautology (the .0 IS the recall). Grounding repair: Why leans on "reliably known" (idx 13's context) and Cue on the last-digit convention (idx 12) — cited both (all yellow marks, no R36 issue) instead of leaving back-lines unsupported by the cited context (check 3). Editor-verified verbatim vs the EXACT highlight → verified_against recorded. |
| 15 | ×/÷ sig-fig rule (idx 16) | **PASS** | 3-word crisp cloze, cold-solves (bolded ×/÷ discriminator forces "fewest significant figures" over the +/− decimal-places answer). Ex arithmetic recomputed: 11.3 × 6.8 = 76.84 ✓ and the factors reconstruct from the context's own bounds (11.2×6.7 / 11.4×6.9). Add/subtract rule correctly Distinguish-only (unmarked, rule 29). |
| 16 | precision — two-way def (idx 18) | **PASS** | c2 = 6-word crisp span ✓ verbatim-faithful ("repeatability… using a given instrument"). **Clearance (check 3):** Distinguish's "a ruler built with a 2% error still gives repeatable (precise) readings that are all slightly off" is the standard implication the cited passage juxtaposes (it is WHY the book's accuracy example is a miscalibrated ruler) — fair inference, non-numeric claim, kept. Mutual Distinguish with #17 = endorsed confusable cross-link. |
| 17 | accuracy — two-way def (idx 19) | **PASS** | Verbatim-grounded; ±0.2 cm Ex checks out (2% of 8.8 ≈ 0.18). The soft c1 direction ("how close to true value" → a lay reader might guess "accuracy") is the two-way definition pattern that rule 3 explicitly forbids "fixing" — stand down, the c2 direction carries the load and the pair discrimination vs precision is the point. Cue (± covers both) grounded ✓, single placement. |

## Cross-card findings (checks 16, 12; Distinguish symmetry)

- **Sibling leaks (16):** the only scenario/classify card (#0) shares no exemplar with any sibling. Definition-family backs deliberately state each other's contrasts (theory/model/law/principle; estimated/percent uncertainty; precision/accuracy) — the endorsed confusable-pair cross-linking, all post-answer. Two reductions made anyway: A3's back no longer near-verbatim spells A5's c2 answer (model clause cut — primarily a grounding fix), and the 80-km family's three backs reveal none of each other's tested counts/notation.
- **Duplicates (12):** no two notes test the same claim; the percent-uncertainty fold-in is the anti-duplicate move. Checker's near-duplicate detector: silent.
- **Distinguish symmetry:** theory↔model on #4; theory↔law on #2/#5; law↔principle on #5/#6; estimated↔percent on #8/#9; precision↔accuracy on #16/#17 — all mutually consistent, no contradictions. interpolate↔extrapolate one-sided by nature (no extrapolate card exists).
- **Cross-block:** no A-block answer is spelled on a B-block card or vice versa; no claim overlap between the blocks.

## Warning clearances & adjudications (named per detector / check)

- `check_cards.py` mechanical pass: **zero warnings** on all 18 cards (grounding R13, lexicon contract, husk/leak/load/quantity detectors all silent). The clearances below are editor-level judgments the mechanics can't see:
  - **husk (check 19), #10:** both c1 spans hidden together still leave a real scenario stem + slot-label hints — cleared as a legitimate hinted pair (detector itself is silent here; logged because I ran the litmus by hand).
  - **forced-choice hint (checks 9/13), #0:** `::classical/modern` contains its answers by design — the rule-4 licensed forced-choice form, not a hint leak.
  - **lexicon authored-content (check 30), #7:** extrapolate contrast = lane-licensed authored knowledge on an external-anchored card (surfaced to Parker via R35→Vocabulary block).
  - **fair-inference grounding (check 3), #16:** 2%-ruler repeatability implication — kept, reasoning above.
  - **numeric flags:** true only where the tested answer is a value/count/date (#1, #12, #13, #14); #9's ×100 and #10's "one or a few units" adjudicated structural/fuzzy-verbal, false — matches the drafter's stated policy, now an editor decision.
  - **verified_against stamps (#12, #13, #14):** values sit verbatim in EXACT-grounded highlight text — editor-verified so verify_report routes them to "Section B: verified, skim" instead of full flag tier (same treatment the drafter gave #1). **#9 deliberately NOT stamped** (its idx 11 is PARTIAL — full human-check tier is correct there).

## Expected errors left for the consolidator (do not "fix")

- `#7` and `#8` R35 hard errors ("external anchor without needs_human_check") are the **expected class** — run `verify_report.py` to derive the flags, then re-gate. Also expect derivations for #5/#9 (PARTIAL-grounded citations). No `.verified` stamp exists for the edited file until then (checker removes/withholds it on hard errors — correct behavior).

## Still for the consolidator

1. **Attach The School of Athens** to #1 (`image_side` already set to `"front"`; leave the painting itself unlabeled by dates — it carries none). Full clean plate at high quality per Parker's comment and the figure-quality preference; if a full-quality plate can't be produced, ship no plate and say so.
2. **Record `perc_uncertaint` in the lexicon ledger as covered-by-fold-in** (#9), so a future re-mark of the term is recognized (drafter requested; seconded).
3. **Vocabulary block:** #7 and #8 (external anchors) must appear in the report for Parker's review; `interpolat` note: the book's own parenthetical gloss ("estimate (or 'interpolate')") supports the authored answer even though no evidence entry exists.
4. Idx 5's `list_lead_in: true` looks like an extractor false positive (no enumeration follows) — drafter's read confirmed by my own pass of pp. 4–5 context; nothing to complete.
