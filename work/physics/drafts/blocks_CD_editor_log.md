# Blocks C + D — adversarial editor log

Editor run 2026-08-26. Input: `block_C.json` (35) + `block_D.json` (3). Output: `blocks_CD_edited.json` (38 cards, C order then D).
Every card ran the full checklist; checks 18–24 ran per row; all 20 prefix values re-verified digit-by-digit against the p31 render's
authoritative table (this run) — **20/20 match, symbols and case included**. Drafter notes were read only after forming an independent
view of each card. Verdicts: **30 PASS · 8 REWRITE · 0 DROP**. Indices below are positions in `blocks_CD_edited.json`.

## Per-card verdicts

- **#0 M1 meter/Academy/1790s (idx 22)** — REWRITE (minor). Back Extra "Why:" relabeled "Ex:" — the platinum-rod line is a follow-on fact, not a reason (check 11 label honesty). Cold-solve passes on all three sub-cards; `::decade` satisfies check 29 on the bare-date blank. Drafter's "measurement standard" (not "standard of length") upheld — it keeps c1 non-decodable (check 15).
- **#1 M2 original meter definition (idx 23)** — REWRITE (provenance only). Text untouched; c1/c2 split under different numbers is the correct anti-husk shape (check 19). The Ex fact (survey off by one-fiftieth of 1%) is idx 23's own dagger footnote whose TEXT sits in idx 26's context window — from_idx extended to [23, 26] so grounding check 3 is auditable instead of a drafter footnote.
- **#2 M3 1983 meter definition (idx 24)** — REWRITE. (a) Fraction 1/299,792,458 verified digit-for-digit against idx 24's context (CJK slash 兾 normalized — correct). (b) Family-leak fix (checks 15/16): the Pathway line revealed sibling answers — "1790s" (M1.c3), "equator-to-pole" (M2.c2), and "platinum-iridium" (K1.c2) — on this card's back; rewritten to "physical rod → engraved marks on a metal bar (1889) → krypton-86 wavelengths (1960) → light travel time (1983, current)", which keeps the era chain and reveals no clozed answer (the alloy-identity teaching lives on K1's own back, its proper home per rule 3). Also dropped the invented word "quadrant" (check 3). (c) from_idx → [24, 23]: 1889/1960/rod live in idx 23's context. c2's bare fraction is announced by the trailing unit frame "of a second" (rule 27 exemption) — no hint needed.
- **#3 S1 second/cesium (idx 25+26)** — REWRITE (minor). Distinguish arithmetic was dimensionally garbled ("24 h × 60 min × 60 s = 86,400 s") — restored source form "24 h/day × 60 min/h × 60 s/min = 86,400 s/day" (check 3). Caps "OLD" → italics per house emphasis style. Back-Extra constants 9,192,631,770 and 1/86,400 verified verbatim against idx 26's context (recorded in verified_by). Both sub-cards cold-solve; no family leaks.
- **#4 K1 kilogram/cylinder (idx 27+28)** — REWRITE (provenance + style). The bar-vs-cylinder Distinguish is the right interference-breaker (check 11) but its 1889-bar fact grounds in idx 23's context — from_idx → [27, 28, 23] (check 3). Caps BAR/CYLINDER → bold. All three sub-cards cold-solve; "near Paris" stays visible as situational cue, not a leak.
- **#5 K2 amu (idx 29)** — PASS. Verified the mandated design held: 1 u = 1.6605 × 10⁻²⁷ kg sits in Back Extra, NOT clozed; digits match idx 29's context and the p31 render. Single-blank term+abbreviation cloze is right (either half visible would decode the other, check 2).
- **#6 U1 metric multiples of 10 (idx 30)** — REWRITE. Back Extra restated kilo=1000 m, centi=1/100 m, milli=1/1000 m, centiliter=1/100 L, kilogram=1000 g — five statements of sibling prefix-row ANSWERS in prose (checks 16/27; rule 25's back-leak clause; the editor brief's explicit no-prefix-values-on-sibling-backs directive). The drafter's defense ("anchor card, not a row card") rejected: the leak mechanism doesn't care which card teaches the value ahead of the row card. Replaced with the grounded value-free line (prefixes attach to any unit — centiliter, kilogram named without values). `::number` hint on the bare "10" upheld (check 29).
- **#7–#26 P1–P20 prefix rows (idx 32)** — REWRITE ×20 (one uniform fix). All 20 values verified digit-by-digit against the authoritative table: yotta 10²⁴ · zetta 10²¹ · exa 10¹⁸ · peta 10¹⁵ · tera 10¹² · giga 10⁹ · mega 10⁶ · kilo 10³ · hecto 10² · deka 10¹ · deci 10⁻¹ · centi 10⁻² · milli 10⁻³ · micro 10⁻⁶ · nano 10⁻⁹ · pico 10⁻¹² · femto 10⁻¹⁵ · atto 10⁻¹⁸ · zepto 10⁻²¹ · yocto 10⁻²⁴ — **all correct, zero wrong exponents**. Fix: added `::power of ten` slot hint to every value blank (check 29 / rule 27): "multiplies a unit by ___" announces a number but not the power-of-ten FORM, and the drafter's "10^ shape announces the slot" defense is wrong — the 10^ shape is hidden until reveal. Rule-25 per-key structure upheld (one note per key, no Roster prose, table plate to be attached by the consolidator). Back-Extra Ex lines checked: each restates only its OWN value (boundary-aware sweep confirmed zero cross-row values on any visible side or Back Extra); all unit arithmetic correct. Mega's 8.2-megapixel Ex grounds in idx 30's context — from_idx → [32, 30] (check 3). Also normalized every 10^N to Unicode superscripts (10²⁴ …) across both blocks for book-like rendering, matching block D's own [L²] style.
- **#27 micro → μ (idx 32)** — PASS. Not first-letter-derivable, so a production card is right; Distinguish names milli's m with no value (check 16 clean); footnote "μ is Greek mu" grounded in idx 32's context.
- **#28 deka → da (idx 32)** — PASS. "Only two-letter abbreviation" claim re-verified against all 20 rows (true; μ is one letter) — it is a form anchor, not a content leak (check 9). Distinguish names deci's d with no value.
- **#29 case-pattern (idx 32)** — REWRITE. The brief's named trap, confirmed: visible "(10^6)" and "(10^3)" handed the mega and kilo row cards' exact answers to a non-knower on a sibling's visible side (checks 15/16). Drafter's "accepted trade-off / most universally known rows" defense rejected — universality is not a leak license. Exponents stripped; boundary anchors are the prefix NAMES, which the case question needs anyway. Forced-choice hints verified against answer spelling exactly ("uppercase/lowercase" = the answers, check 13); 2 complementary fills under one number is one honest retrieval (check 25). Pattern itself re-verified against the table: M G T P E Z Y upper; k h da d c m (μ) n p f a z y lower.
- **#30 lexicon unit (idx 20)** — PASS. Plain · crisp (8 w) · faithful; Ex present, sense matches. The one in-source evidence hit ("unit (u or amu)") is the ATOMIC-MASS-UNIT sense, not the generic p29 sense Parker marked — the drafter's downgrade-to-external is correct (check 30's sense trap, independently confirmed against `lexicon_evidence.json`); clearance already recorded in verified_by. Mirrored unit↔standard Distinguish lines are the sanctioned confusable-pair treatment and agree with #31's.
- **#31 lexicon standard (idx 21)** — PASS. Plain · crisp (8 w) · faithful ("the defining reference" discriminator kept; "physical reference" covers artifact and phenomenon standards). No blend with operational definition (#34 is rule/procedure). Cylinder/kilogram example on its back adjudicated as reinforcement, not a check-16 leak (no scenario/classify card in the batch; #4's blanks still demand production).
- **#32 lexicon base quantity (idx 31)** — PASS. 9-word blank warning: clearance UPHELD (the tail "not other quantities" is the discriminator vs derived; drafter's clearance stands in verified_by). "Seven" on the Why line grounds in idx 31's context; correctly NOT carded as its own fact (unmarked — rule 29).
- **#33 lexicon derived quantities (idx 33)** — PASS. Genus word "quantities" repeats but the jargon root "derived" is not recycled (check 30's plain test); speed = distance ÷ time grounds in idx 33's own context; Distinguish mirrors #32 consistently.
- **#34 lexicon operational definition (idx 34)** — PASS. Crisp (7 w), faithful; Parts: line instead of a Distinguish-vs-standard is right — a contrast line there invited exactly the blending check 30 hunts.
- **#35 lexicon order-of-magnitude estimate (idx 35)** — PASS. Plain · crisp (7 w) · faithful ("rounded to powers of ten" renders the jargon root); Ex + Distinguish + Formal all verbatim-grounded in idx 35's context (margin-box interleave correctly removed); external anchor verified against the evidence file (no entry). verified_against normalized "p13" → "p34" (see conventions below).
- **#36 dimensions definition (idx 36)** — PASS. Endorsed two-way shape; both directions cold-solve; velocity/[L/T] Distinguish is a fresh exemplar that never states [L²] (check 16 clean vs #37). "In physics," megadeck frame upheld. verified_against "p16" → "p37".
- **#37 area dimensions [L²] (idx 37+36)** — PASS. [L²] reconstruction from garbled "CL2 D" independently confirmed via idx 36's intact bracket convention; the visible "the units vary, not the dimensions" tail is a rule-21(b) contrast anchor that names no dimension (no leak, and it closes the lone-"always" open-set risk, check 23). verified_against "p16" → "p37".

## Cross-card findings

1. **Prefix-value isolation (checks 16/25–27): CLEAN after edits.** Mechanical boundary-aware sweep over all 38 cards: no prefix exponent appears on any card's visible side or Back Extra except the owning row card's own value. The two violations found and fixed: #29's stem (mega/kilo exponents) and #6's Back Extra (kilo/centi/milli values in prose). The full-table PLATE on row-card backs (consolidator attaches) is Parker's own rule-25 design and is not prose restatement.
2. **Meter/second/kilogram family isolation: CLEAN after edits.** #2's Pathway was the one leak (three sibling answers; fixed). Remaining cross-mentions are unclozed facts (rod, 1889 bar, krypton-86, 2.2 lb) — none is any card's answer.
3. **Symbols visible on row cards (e.g. "micro (μ)", "deka (da)") while #27/#28 test their production** — adjudicated NOT a check-16 violation: re-exposure of notation on entity-labels is ordinary interleaved reinforcement, and stripping symbols from just those two rows would itself mark them as special (a meta-leak). Same reasoning clears "meter"/"second"/"kilogram" appearing as visible anchor words across the batch.
4. **Dedupe (rule 12): no duplicate claims across C and D.** unit/standard, base/derived, and #36/#37 are distinct-fact pairs with consistent mirrored Distinguish lines (verified line-by-line).
5. **Case-pattern ↔ row cards**: after the #29 fix neither direction self-answers; row cards showing their symbol's case is the raw material the pattern card generalizes, not a giveaway.
6. **Under-clozing adjudications (check 4):** 18 first-letter-derivable symbols deliberately untested (organizing-principle design: #29 + the two exception cards — upheld); "1983" visible in #2 (unmarked scaffolding date, not the marked fact); "seven base quantities" Back-Extra-only (unmarked — rule 29); "makes calculation easy" in #6 visible (soft benefit, not crisp fact — rule 1).

## Warning clearances (recorded per note-format)

- `long_cloze (R8)` on **#32**: cleared — 9 plain words is the floor that keeps the mandated discriminator ("its own standard, not other quantities"); recorded in the card's verified_by (drafter) and upheld by this editor.
- `lexicon in-source-exists` on **#30**: cleared — the evidence quote "unit (u or amu)" is the atomic-mass-unit sense, not the generic sense Parker marked; §4b sense-check downgrade to external stands; recorded in the card's verified_by. Do not re-anchor.
- The **6 HARD R35 errors (#30–#35, external anchor without needs_human_check)** are the expected pre-verify state my brief names: `needs_human_check` is derived, never asserted — consolidator runs `verify_report.py` and they resolve into the report's Vocabulary block.
- No `unlabeled_quantity_blank`, husk, sibling-split, overloaded-group, quantitative-panel, or row-tautology warnings fired on the final file.

## Conventions normalized / consolidator notes

- **verified_against = PDF page ("pNN" matching `page_NN.png`)**, per block A's shipped precedent ("p23" = page_23.png) and block C. Block D's label-based "p13"/"p16" were actively misleading (a later render pass would open the wrong page) — normalized to "p34"/"p37" with the label noted in verified_by.
- **Exponent style normalized to Unicode superscripts** (10²⁴, 10⁻²⁷) in Text and Back Extra across both blocks — matches D's [L²], avoids MathJax-inside-cloze risk, reads like the book.
- **numeric-field convention followed as drafted** (numeric = clozed answer is a number). #3's and #5's Back-Extra precision constants were digit-verified by this editor against their cited contexts and the p31 render respectively (recorded in verified_by); #2 (fraction), #0 (1790s), #1 (ten-millionth), #6 remain numeric=true with verified_against null, so verify_report will correctly route them to Parker's skim — the designed conservatism; a p30 render pass could clear #2 if the consolidator wants it off the skim list.
- Still owed downstream (drafter flags upheld): force-attach the TABLE 1-4 plate to backs of #7–#26; run lexicon.py ledger/liveness dedup on the six lexicon terms; surface the MIDWORD hygiene flags on idx 20/21 and the reverse-direction (value → prefix name) open question at hand-off; one permitted hand-off sentence about unmarked p31 SI/MKS/cgs prose and Table 1-5.
